# 🚀 DEPLOY CHECKLIST - PROMO.FM Concerts Feature

## ✅ ПРЕ-ДЕПЛОЙ (Обязательно!)

### 1. Проверка файлов
- [x] `/ARCHITECTURE.md` - документация создана
- [x] `/supabase/migrations/20260126_create_concerts_tables.sql` - миграция готова
- [x] `/src/types/database.ts` - типы созданы
- [x] `/supabase/functions/server/concerts-routes.tsx` - API роуты созданы
- [x] `/src/services/concerts-api.ts` - API сервис создан
- [x] `/src/services/concerts-api-adapter.ts` - Fallback адаптер создан
- [x] `/src/lib/supabase.ts` - Supabase client helper создан
- [x] `/src/app/components/my-concerts-page.tsx` - новый компонент создан
- [x] `@supabase/supabase-js` - пакет установлен

### 2. Backend подготовка

#### Опция A: С Postgres (рекомендуется)
```bash
# 1. Открыть Supabase Dashboard
https://app.supabase.com/project/[your-project-id]/editor

# 2. SQL Editor -> New query
# 3. Скопировать весь SQL из:
/supabase/migrations/20260126_create_concerts_tables.sql

# 4. Выполнить миграцию
# 5. Проверить что таблицы созданы:
SELECT * FROM tour_dates LIMIT 1;
SELECT * FROM artist_profiles LIMIT 1;

# 6. Проверить что RLS политики созданы:
SELECT * FROM pg_policies WHERE tablename IN ('tour_dates', 'artist_profiles');
```

#### Опция B: Без Postgres (временно)
```
✅ Ничего делать не нужно!
Адаптер автоматически будет использовать mock данные
```

### 3. Проверка Edge Functions

```bash
# Убедиться что concerts-routes подключены в index.tsx
# Файл: /supabase/functions/server/index.tsx

# Должно быть:
import concertsRoutes from "./concerts-routes.tsx";
app.route("/make-server-84730125/api/concerts", concertsRoutes);
```

---

## 🔧 ДЕПЛОЙ ПРОЦЕСС

### Шаг 1: Деплой Backend (Supabase Edge Functions)

```bash
# Если используете Supabase CLI:
supabase functions deploy server

# Или через Vercel (если Edge Functions интегрированы)
# Просто пуш в репозиторий
git add .
git commit -m "feat: add concerts feature with Postgres support"
git push origin main
```

### Шаг 2: Деплой Frontend (Vercel)

```bash
# Frontend автоматически задеплоится при пуше
# Или форсировать деплой:
vercel --prod

# Проверить что билд прошел успешно
# Проверить что нет TypeScript ошибок
```

### Шаг 3: Проверка после деплоя

**A. Проверить API endpoints:**
```bash
# Health check
curl https://[project-id].supabase.co/functions/v1/make-server-84730125/health

# Concerts API (без auth - вернет 401, это ок)
curl https://[project-id].supabase.co/functions/v1/make-server-84730125/api/concerts/tour-dates
```

**B. Проверить Frontend:**
1. Открыть приложение
2. Перейти в раздел "Мои Концерты"
3. Должны загрузиться данные (либо из Postgres, либо mock)
4. Попробовать создать концерт
5. Попробовать удалить концерт

---

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема 1: "Network error" при загрузке

**Решение:**
```typescript
// Проверить в Browser Console:
// 1. Есть ли CORS ошибки?
// 2. Правильный ли URL для API?
// 3. Есть ли access token?

// Временное решение: адаптер автоматически переключится на mock данные
```

### Проблема 2: "Unauthorized" (401)

**Решение:**
```typescript
// Проблема: нет auth токена
// Решение: реализовать полноценную авторизацию или использовать mock режим

// В файле: /src/lib/supabase.ts
// Убедиться что projectId и publicAnonKey правильные
```

### Проблема 3: Таблицы не существуют

**Решение:**
```sql
-- Заново выполнить миграцию в Supabase Dashboard
-- SQL Editor -> New query -> Вставить SQL из миграции -> Run

-- Проверить что таблицы созданы:
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('tour_dates', 'artist_profiles');
```

### Проблема 4: RLS блокирует запросы

**Решение:**
```sql
-- Временно отключить RLS (ТОЛЬКО ДЛЯ ТЕСТИРОВАНИЯ!)
ALTER TABLE tour_dates DISABLE ROW LEVEL SECURITY;
ALTER TABLE artist_profiles DISABLE ROW LEVEL SECURITY;

-- После тестирования ОБЯЗАТЕЛЬНО включить обратно!
ALTER TABLE tour_dates ENABLE ROW LEVEL SECURITY;
ALTER TABLE artist_profiles ENABLE ROW LEVEL SECURITY;
```

### Проблема 5: TypeScript ошибки при билде

**Решение:**
```bash
# Проверить типы локально
npm run type-check

# Если есть ошибки - исправить их перед деплоем
# Убедиться что все импорты правильные
```

---

## 📊 POST-DEPLOY ПРОВЕРКА

### 1. Функциональность

- [ ] Страница "Мои Концерты" загружается
- [ ] Отображаются концерты (реальные или mock)
- [ ] Можно создать новый концерт
- [ ] Можно редактировать концерт
- [ ] Можно удалить концерт
- [ ] Можно отправить на модерацию
- [ ] Можно продвинуть концерт за коины
- [ ] Статусы отображаются правильно
- [ ] Даты форматируются корректно
- [ ] Анимации работают плавно

### 2. Performance

- [ ] Страница загружается < 3 секунд
- [ ] Нет memory leaks
- [ ] Нет консольных ошибок
- [ ] Responsive на всех устройствах

### 3. Backend

- [ ] API отвечает < 1 секунды
- [ ] RLS политики работают
- [ ] Auth проверяется корректно
- [ ] Ошибки логируются правильно

---

## 🎯 РЕЖИМЫ РАБОТЫ

### Режим 1: Полноценный Postgres ✅ (Целевой)
```
Frontend -> API Service -> Edge Functions -> Postgres
- Реальные данные
- Полная безопасность
- Масштабируемость
- Persistence
```

### Режим 2: Mock режим 🔄 (Fallback)
```
Frontend -> API Adapter -> Mock Data (in memory)
- Тестовые данные
- Работает без backend
- Для разработки
- Не persistent
```

**Текущий режим определяется автоматически!**

---

## 📝 ROLLBACK ПЛАН

Если что-то пошло не так:

### Option 1: Откатить frontend
```bash
# Vercel Dashboard -> Deployments -> Previous deployment -> Promote to Production
```

### Option 2: Откатить миграцию
```sql
-- В Supabase Dashboard SQL Editor:
DROP TABLE IF EXISTS tour_dates CASCADE;
DROP TABLE IF EXISTS artist_profiles CASCADE;
DROP FUNCTION IF EXISTS create_artist_profile_on_signup CASCADE;
DROP FUNCTION IF EXISTS update_updated_at_column CASCADE;
```

### Option 3: Отключить новый функционал
```typescript
// В App.tsx заменить:
import { MyConcertsPage } from './components/my-concerts-page';
// На:
import { ConcertsPage } from './components/concerts-page'; // старый компонент
```

---

## 🎉 SUCCESS CRITERIA

Деплой считается успешным если:

✅ Приложение загружается без ошибок  
✅ Можно открыть раздел "Мои Концерты"  
✅ Данные отображаются (реальные или mock)  
✅ CRUD операции работают  
✅ Нет критических ошибок в console  
✅ Нет 500 ошибок от backend  
✅ Performance приемлемый  

---

## 📞 SUPPORT

При проблемах:

1. **Проверить Browser Console** - там будут все ошибки
2. **Проверить Network Tab** - посмотреть что API возвращает
3. **Проверить Supabase Logs** - Dashboard -> Logs -> Edge Functions
4. **Проверить документацию** - `/ARCHITECTURE.md`
5. **Использовать mock режим** - если Postgres не работает

---

## 🔮 СЛЕДУЮЩИЕ ШАГИ

После успешного деплоя:

1. [ ] Добавить форму создания/редактирования концертов
2. [ ] Реализовать Performance History раздел
3. [ ] Добавить календарь событий
4. [ ] Интегрировать с главным меню
5. [ ] Добавить уведомления
6. [ ] Создать публичную страницу концертов
7. [ ] Добавить фильтры и поиск
8. [ ] Реализовать экспорт в календарь
9. [ ] Добавить интеграцию с соцсетями
10. [ ] Настроить аналитику

---

**Дата создания:** 26 января 2026  
**Версия:** 1.0  
**Статус:** Ready for Deploy ✅
