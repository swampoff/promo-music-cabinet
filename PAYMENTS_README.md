# 💰 СИСТЕМА ПЛАТЕЖЕЙ PROMO.MUSIC

**Версия:** 1.0.0  
**Дата:** 27 января 2026  
**Статус:** ✅ Готово к деплою

---

## 🎯 ВОЗМОЖНОСТИ

### ✨ Что умеет система:

1. **Транзакции**
   - Отслеживание всех доходов и расходов
   - Детализация 18+ полей
   - Фильтрация и поиск
   - Раскрываемые карточки

2. **Источники дохода**
   - 💜 Донаты от фанатов
   - 🎵 Концерты и выступления
   - 📻 Радио и стриминг
   - 🎫 Продажа билетов
   - ☕ Отчисления от заведений

3. **Расходы**
   - 📢 Маркетинг и реклама
   - 🪙 Покупка коинов
   - 👑 Подписка Pro
   - 🎯 Питчинг треков
   - 🏷️ Баннеры

4. **Функционал**
   - Баланс в реальном времени
   - История транзакций
   - Методы оплаты
   - Вывод средств
   - Статистика и аналитика

---

## 📁 СТРУКТУРА ПРОЕКТА

```
/src/app/
  ├── data/
  │   └── transactions-data.ts          # Данные транзакций (прототип)
  ├── components/
  │   ├── payments-page.tsx             # Главная страница
  │   ├── transaction-detail-card.tsx   # Карточка с детализацией
  │   ├── donations-page.tsx            # Интеграция с донатами
  │   └── subscription-page.tsx         # Интеграция с подпиской

/supabase/
  ├── migrations/
  │   └── 20260127_payments_system.sql  # SQL миграция для деплоя
  └── functions/server/
      ├── payments.ts                   # Бизнес-логика
      ├── payments-routes.tsx           # API endpoints
      └── index.tsx                     # Главный сервер
```

---

## 🚀 БЫСТРЫЙ СТАРТ

### 1. Запуск прототипа (без БД):

```bash
# Установить зависимости
npm install

# Запустить dev сервер
npm run dev

# Открыть браузер
http://localhost:5173

# Перейти в раздел
Платежи и финансы → любая вкладка
```

### 2. Деплой в Supabase:

```bash
# 1. Применить SQL миграцию
supabase db push

# 2. Деплой Edge Functions
supabase functions deploy make-server-84730125

# 3. Проверить здоровье API
curl https://YOUR_PROJECT.supabase.co/functions/v1/make-server-84730125/health
```

### 3. Подключение к реальной БД:

См. файл `/PAYMENTS_INTEGRATION_CHECK.md` раздел "Настройка для Production"

---

## 📊 API ENDPOINTS

### Транзакции:
```
GET  /make-server-84730125/payments/transactions?user_id=xxx
POST /make-server-84730125/payments/transactions
```

### Баланс:
```
GET /make-server-84730125/payments/balance?user_id=xxx
GET /make-server-84730125/payments/stats?user_id=xxx
GET /make-server-84730125/payments/category-stats?user_id=xxx&type=income
```

### Методы оплаты:
```
GET  /make-server-84730125/payments/payment-methods?user_id=xxx
POST /make-server-84730125/payments/payment-methods
```

### Вывод средств:
```
GET  /make-server-84730125/payments/withdrawals?user_id=xxx
POST /make-server-84730125/payments/withdrawals
```

### Синхронизация:
```
POST /make-server-84730125/payments/sync/donation
POST /make-server-84730125/payments/sync/subscription
```

---

## 💾 БАЗА ДАННЫХ

### Таблицы:

1. **make_transactions_84730125** - Все транзакции
2. **make_payment_methods_84730125** - Методы оплаты
3. **make_withdraw_requests_84730125** - Заявки на вывод
4. **make_user_balances_84730125** - Балансы пользователей

### Функции:

1. **create_transaction_84730125()** - Создание транзакции с автоматическим расчётом комиссии
2. **create_withdraw_request_84730125()** - Создание заявки на вывод с проверкой баланса
3. **get_user_stats_84730125()** - Получение статистики пользователя

### Триггеры:

1. **trigger_update_user_balance** - Автоматическое обновление баланса
2. **trigger_*_updated_at** - Автоматическое обновление времени изменения

---

## 🔗 ИНТЕГРАЦИИ

### 1. С донатами:
```tsx
// Донаты автоматически создают транзакции
// Кнопка "Ответить донатеру" открывает мессенджер
```

### 2. С подпиской:
```tsx
// Платёж за подписку создаёт транзакцию расхода
// Показывается в разделе "Расходы"
```

### 3. С мессенджером:
```tsx
// Клик на "Ответить донатеру" → переключение на раздел "Сообщения"
// Передаётся userId, userName, userAvatar
```

---

## 📱 АДАПТИВНОСТЬ

| Устройство | Breakpoint | Особенности |
|------------|------------|-------------|
| **Mobile** | < 768px | Вертикальная раскладка, компактные размеры |
| **Tablet** | 768px - 1024px | 2 колонки, средние размеры |
| **Desktop** | > 1024px | 3 колонки, hover эффекты |

---

## 🎨 ФИЧИ

### Детализация транзакций:
- ✅ Клик на транзакцию → раскрытие
- ✅ 18+ полей информации
- ✅ Финансовая сводка (валовая, комиссия, чистая)
- ✅ Специфичные данные по категориям
- ✅ Копирование ID транзакции
- ✅ Скачивание чека

### Специфичные данные:

**🎫 Продажа билетов:**
- Событие, площадка, дата
- Количество билетов, цена
- Общая выручка

**☕ Отчисления от заведений:**
- Список треков (с иконками)
- Список заведений (с адресами)
- Количество проигрываний
- Период отчётности

**💜 Донаты:**
- Полное сообщение
- Кнопка "Ответить донатеру"

**🪙 Коины:**
- Количество купленных/потраченных

**👑 Подписка:**
- Период подписки
- Дата следующего списания

---

## 📖 ДОКУМЕНТАЦИЯ

### Полная документация:
1. `/NEW_INCOME_SOURCES.md` - Новые источники дохода
2. `/DETAILED_TRANSACTIONS_ADAPTIVE.md` - Детализация и адаптив
3. `/FINAL_SUMMARY_TRANSACTIONS.md` - Финальный отчёт
4. `/VERIFICATION_REPORT.md` - Проверка целостности
5. `/PAYMENTS_INTEGRATION_CHECK.md` - Проверка всех связей
6. `/PAYMENTS_README.md` - Этот файл

### SQL миграция:
- `/supabase/migrations/20260127_payments_system.sql`

---

## 🛠️ РАЗРАБОТКА

### Добавление нового типа транзакции:

1. **Обновить enum в SQL:**
```sql
ALTER TYPE transaction_category ADD VALUE 'new_category';
```

2. **Добавить иконку:**
```tsx
case 'new_category': 
  return <NewIcon className="w-4 h-4 text-color-400" />;
```

3. **Добавить специфичную детализацию:**
```tsx
{transaction.category === 'new_category' && (
  <div>
    {/* Специфичные поля */}
  </div>
)}
```

### Добавление нового API endpoint:

1. **Создать функцию в payments.ts:**
```typescript
export async function newFunction(userId: string) {
  // Логика
}
```

2. **Добавить роут в payments-routes.tsx:**
```typescript
paymentsRoutes.get('/new-endpoint', async (c) => {
  const result = await newFunction(userId);
  return c.json({ success: true, data: result });
});
```

---

## 🐛 TROUBLESHOOTING

### Транзакции не загружаются:
```bash
# Проверить API
curl https://YOUR_PROJECT.supabase.co/functions/v1/make-server-84730125/health

# Проверить БД
SELECT * FROM make_transactions_84730125 LIMIT 10;

# Проверить логи
supabase functions logs make-server-84730125
```

### Баланс не обновляется:
```sql
-- Проверить триггер
SELECT * FROM pg_trigger WHERE tgname = 'trigger_update_user_balance';

-- Проверить баланс вручную
SELECT * FROM make_user_balances_84730125 WHERE user_id = 'artist_demo_001';
```

### RLS блокирует запросы:
```sql
-- Временно отключить (только для дебага!)
ALTER TABLE make_transactions_84730125 DISABLE ROW LEVEL SECURITY;

-- Проверить политики
SELECT * FROM pg_policies WHERE tablename = 'make_transactions_84730125';
```

---

## 📈 СТАТИСТИКА

| Метрика | Значение |
|---------|----------|
| **Файлов создано** | 7 |
| **Строк кода** | 2000+ |
| **API endpoints** | 10+ |
| **Таблиц в БД** | 4 |
| **Функций SQL** | 3 |
| **Триггеров** | 4 |
| **Категорий транзакций** | 9 |
| **Источников дохода** | 7 |
| **Breakpoints адаптива** | 3 |

---

## ✅ ЧЕКЛИСТ

### Перед деплоем:
- [ ] SQL миграция применена
- [ ] Edge Functions задеплоены
- [ ] API endpoints проверены
- [ ] RLS политики настроены
- [ ] Демо данные загружены
- [ ] Frontend подключён к API
- [ ] Тесты пройдены

### Перед production:
- [ ] Webhook для платёжных систем настроен
- [ ] Email уведомления настроены
- [ ] Мониторинг настроен
- [ ] Резервное копирование настроено
- [ ] Rate limiting настроен
- [ ] SSL сертификаты установлены

---

## 🤝 КОНТРИБЬЮШН

Для добавления новых фич:

1. Создайте ветку: `git checkout -b feature/new-payment-type`
2. Внесите изменения
3. Обновите документацию
4. Создайте PR

---

## 📞 ПОДДЕРЖКА

**Проект:** promo.music  
**Версия:** 1.0.0  
**Дата релиза:** 27 января 2026

**Документация:** См. файлы в корне проекта  
**SQL миграция:** `/supabase/migrations/20260127_payments_system.sql`  
**API документация:** См. `/PAYMENTS_INTEGRATION_CHECK.md`

---

**Made with ❤️ for musicians**

**Автор:** AI Assistant  
**Дата:** 27 января 2026
