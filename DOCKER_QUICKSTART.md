# 🚀 Quick Start - Docker для Promo.Music

Быстрый запуск приложения локально через Docker за 3 минуты.

## 📦 Требования

- Docker Desktop установлен и запущен
- 8GB RAM на компьютере
- 10GB свободного места

## ⚡ Быстрый запуск

### Linux / macOS

```bash
# 1. Сделайте скрипты исполняемыми
chmod +x *.sh

# 2. Запустите
./docker-start.sh
```

### Windows

```powershell
# Запустите PowerShell как администратор
.\docker-start.ps1
```

## 🎯 После запуска

**Откройте в браузере:**
- 🎨 Frontend: http://localhost:5173
- 🗄️ Supabase Studio: http://localhost:3001

**Готово!** Приложение работает локально.

---

## 🔧 Режим разработки (Frontend отдельно)

Если вы хотите работать с frontend кодом с hot-reload:

### 1. Запустите только backend

**Linux/macOS:**
```bash
./docker-dev.sh
```

**Windows:**
```powershell
.\docker-dev.ps1
```

### 2. В отдельном терминале запустите frontend

```bash
npm install
npm run dev
```

**Откройте:** http://localhost:5173

---

## 📝 Основные команды

### Просмотр логов

```bash
# Linux/macOS
./docker-logs.sh

# Windows
.\docker-logs.ps1
```

### Остановка

```bash
# Linux/macOS
./docker-stop.sh

# Windows
.\docker-stop.ps1
```

### Или напрямую

```bash
# Остановка (данные сохраняются)
docker-compose down

# Остановка + удаление данных
docker-compose down -v

# Просмотр статуса
docker-compose ps

# Логи
docker-compose logs -f
```

---

## 🐛 Проблемы?

### Порт уже занят

```bash
# Linux/macOS - найти и убить процесс на порту 5173
lsof -i :5173
kill -9 <PID>

# Windows PowerShell
Get-Process -Id (Get-NetTCPConnection -LocalPort 5173).OwningProcess | Stop-Process -Force
```

### Контейнеры не запускаются

```bash
# Полная переустановка
docker-compose down -v
docker-compose up -d --build
```

### Приложение не подключается к Supabase

```bash
# Проверьте что Kong работает
curl http://localhost:8000/health

# Перезапустите Kong
docker-compose restart kong
```

---

## 📚 Подробная документация

См. **[DOCKER_README.md](./DOCKER_README.md)** для:
- Полного описания всех сервисов
- Продвинутой конфигурации
- Подключения к базе данных
- Troubleshooting guide

---

## 🎵 Promo.Music Team
