# 🚀 Инструкция по деплою PROMO.MUSIC

## ✅ Код готов к деплою на любой платформе!

Этот проект **НЕ требует** настройки переменных окружения - все работает "из коробки"!

---

## 📦 Деплой на Vercel (рекомендуется)

### Вариант 1: Через GitHub

1. **Создай репозиторий на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ваш-username/promo-music.git
   git push -u origin main
   ```

2. **Импортируй в Vercel:**
   - Открой [vercel.com](https://vercel.com)
   - Нажми "Add New" → "Project"
   - Выбери свой репозиторий
   - Нажми "Deploy" (БЕЗ настройки переменных!)

3. **Готово!** Сайт автоматически соберётся и задеплоится

### Вариант 2: Через Vercel CLI

```bash
npm install -g vercel
vercel
```

Следуй инструкциям в терминале.

---

## 🌐 Деплой на Netlify

### Через Netlify UI:

1. **Перетащи папку проекта** на [app.netlify.com/drop](https://app.netlify.com/drop)
2. **Готово!** Netlify автоматически соберёт проект

### Через Netlify CLI:

```bash
npm install -g netlify-cli
npm run build
netlify deploy --prod --dir=dist
```

---

## ☁️ Деплой на другие платформы

### Railway:
```bash
# Подключи GitHub репозиторий
# Railway автоматически определит Vite проект
```

### Render:
1. Создай новый "Static Site"
2. Build Command: `npm run build`
3. Publish Directory: `dist`

### Cloudflare Pages:
1. Подключи GitHub репозиторий
2. Build Command: `npm run build`
3. Output Directory: `dist`

---

## 🔧 Переменные окружения (опционально)

Проект работает БЕЗ настройки env переменных!

Но если хочешь использовать свой Supabase:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

Добавь их в настройках платформы деплоя.

---

## 🐛 Устранение проблем

### Белый экран после деплоя?

**Решение:** Обнови код из Figma Make - старые версии имели проблемные импорты.

### 404 при переходе по страницам?

**Решение для Vercel** - создай `vercel.json`:
```json
{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
```

**Решение для Netlify** - создай `public/_redirects`:
```
/*    /index.html   200
```

---

## ✨ Локальный запуск

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Превью собранной версии
npm run preview
```

---

## 📋 Требования

- Node.js 18+ 
- npm или pnpm

---

## 🎉 Всё!

Проект использует:
- ✅ React + Vite
- ✅ Tailwind CSS v4
- ✅ Supabase (встроенные ключи)
- ✅ Motion/Framer Motion
- ✅ Lucide React Icons

Никаких дополнительных настроек не требуется!
