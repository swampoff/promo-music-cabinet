# 🔥 РЕАЛИЗАЦИЯ БИЗНЕС-ПРОЦЕССОВ - PROMO.MUSIC

**Дата:** 29 января 2026  
**Статус:** 🚧 В ПРОЦЕССЕ РЕАЛИЗАЦИИ

---

## ❌ КРИТИЧЕСКАЯ ПРОБЛЕМА ОБНАРУЖЕНА

### **Проблема: Данные НЕ синхронизируются между кабинетами**

**Что происходит сейчас:**
1. ✅ Артист загружает трек → трек добавляется в **локальный state** ArtistApp
2. ❌ Админ открывает модерацию → **НЕ ВИДИТ** этот трек (свой state)
3. ❌ Админ одобряет трек → артист **НЕ ВИДИТ** изменение статуса
4. ❌ Финансовые операции **НЕ синхронизируются**

**Архитектурная проблема:**
```
ArtistApp (tracks-page.tsx)
└─ useState<Track[]> - ИЗОЛИРОВАННЫЙ STATE ❌

AdminApp (TrackModeration.tsx)  
└─ useState<Track[]> - ИЗОЛИРОВАННЫЙ STATE ❌

❌ НЕТ ОБЩЕГО ХРАНИЛИЩА
❌ НЕТ СИНХРОНИЗАЦИИ
❌ НЕТ ПЕРСИСТЕНТНОСТИ
```

---

## ✅ РЕШЕНИЕ: DataContext - Глобальное хранилище

### **Архитектура:**
```
RootApp
└─ DataProvider (глобальное хранилище)
    ├─ ArtistApp → читает/пишет в DataContext
    └─ AdminApp → читает/пишет в DataContext

✅ ЕДИНЫЙ ИСТОЧНИК ДАННЫХ
✅ АВТОСИНХРОНИЗАЦИЯ
✅ ПЕРСИСТЕНТНОСТЬ (localStorage)
```

### **Что создано:**

#### **1. /src/contexts/DataContext.tsx**
**Функционал:**
- ✅ Управление треками (tracks)
- ✅ Управление видео (videos)
- ✅ Управление концертами (concerts)
- ✅ Управление новостями (news)
- ✅ Управление транзакциями (transactions)
- ✅ Автосохранение в localStorage
- ✅ Автозагрузка из localStorage

**API Context:**
```typescript
// Tracks
addTrack(track)
updateTrack(id, updates)
deleteTrack(id)
getTracksByUser(userId)
getPendingTracks()

// Videos
addVideo(video)
updateVideo(id, updates)
deleteVideo(id)
getVideosByUser(userId)
getPendingVideos()

// Concerts
addConcert(concert)
updateConcert(id, updates)
deleteConcert(id)
getConcertsByUser(userId)
getPendingConcerts()

// News
addNews(newsItem)
updateNews(id, updates)
deleteNews(id)
getNewsByUser(userId)
getPendingNews()

// Transactions
addTransaction(transaction)
updateTransaction(id, updates)
getTransactionsByUser(userId)
getUserBalance(userId)

// Stats
getTotalStats()
```

#### **2. Интеграция в RootApp**
```typescript
<DataProvider>
  <ArtistApp />
  <AdminApp />
</DataProvider>
```

---

## 🔄 БИЗНЕС-ПРОЦЕССЫ (В РАЗРАБОТКЕ)

### **ПРОЦЕСС 1: Загрузка и модерация трека**

#### **Шаг 1: Артист загружает трек**
```typescript
// В tracks-page.tsx
const { addTrack } = useData();

const handleUploadTrack = () => {
  addTrack({
    title: 'My New Track',
    artist: 'Александр Иванов',
    userId: 'artist_123',
    status: 'pending',
    // ... остальные поля
  });
  
  // Трек автоматически сохраняется в DataContext + localStorage
  // Доступен во всех кабинетах!
};
```

#### **Шаг 2: Админ видит трек на модерации**
```typescript
// В TrackModeration.tsx
const { getPendingTracks, updateTrack } = useData();

const pendingTracks = getPendingTracks();
// Админ видит ВСЕ треки со статусом 'pending'
```

#### **Шаг 3: Админ одобряет трек**
```typescript
const handleApprove = (trackId: number) => {
  updateTrack(trackId, { 
    status: 'approved',
    moderationNote: 'Отличный трек!'
  });
  
  // Создаём транзакцию (оплата за загрузку)
  addTransaction({
    userId: track.userId,
    type: 'expense',
    amount: -50, // Списание 50 коинов
    description: `Модерация трека: ${track.title}`,
    status: 'completed',
  });
};
```

#### **Шаг 4: Артист видит результат**
```typescript
// В tracks-page.tsx
const { getTracksByUser } = useData();

const myTracks = getTracksByUser('artist_123');
// Артист видит обновлённый статус 'approved' ✅
```

#### **Шаг 5: Финансы обновляются**
```typescript
// В payments-page.tsx
const { getUserBalance, getTransactionsByUser } = useData();

const balance = getUserBalance('artist_123'); // -50 коинов
const transactions = getTransactionsByUser('artist_123');
// Видит транзакцию "Модерация трека: My New Track"
```

---

### **ПРОЦЕСС 2: Видео (аналогично)**
1. Артист загружает видео → addVideo()
2. Админ видит → getPendingVideos()
3. Админ одобряет → updateVideo()
4. Создаётся транзакция
5. Артист видит обновление

---

### **ПРОЦЕСС 3: Концерты (аналогично)**
1. Артист создаёт концерт → addConcert()
2. Админ видит → getPendingConcerts()
3. Админ одобряет → updateConcert()
4. Создаётся транзакция
5. Артист видит обновление

---

### **ПРОЦЕСС 4: Новости (аналогично)**
1. Артист публикует новость → addNews()
2. Админ видит → getPendingNews()
3. Админ одобряет → updateNews()
4. Артист видит обновление

---

## 📋 СЛЕДУЮЩИЕ ШАГИ

### ✅ **ЧТО ГОТОВО:**
1. ✅ DataContext создан
2. ✅ Интегрирован в RootApp
3. ✅ API для всех сущностей готово
4. ✅ localStorage персистентность

### 🚧 **ЧТО НУЖНО СДЕЛАТЬ:**

#### **1. Интегрировать DataContext в компоненты артиста:**
- [ ] `/src/app/components/tracks-page.tsx`
- [ ] `/src/app/components/video-page.tsx`
- [ ] `/src/app/components/my-concerts-page.tsx`
- [ ] `/src/app/components/news-page.tsx`
- [ ] `/src/app/components/payments-page.tsx`

#### **2. Интегрировать DataContext в компоненты админа:**
- [ ] `/src/admin/pages/TrackModeration.tsx`
- [ ] `/src/admin/pages/VideoModeration.tsx`
- [ ] `/src/admin/pages/ConcertModeration.tsx`
- [ ] `/src/admin/pages/NewsModeration.tsx`
- [ ] `/src/admin/pages/Finances.tsx`

#### **3. Реализовать финансовые операции:**
- [ ] Списание коинов при загрузке контента
- [ ] Начисление коинов при одобрении
- [ ] Отображение транзакций
- [ ] Обновление баланса

#### **4. Добавить ID пользователя:**
- [ ] Хранить userId в localStorage при логине
- [ ] Передавать userId во все операции
- [ ] Фильтровать данные по userId

#### **5. Тестирование полного цикла:**
- [ ] Загрузка трека → модерация → оплата → отображение
- [ ] Загрузка видео → модерация → оплата → отображение
- [ ] Создание концерта → модерация → оплата → отображение
- [ ] Публикация новости → модерация → отображение

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После интеграции DataContext:

✅ **Артист загружает трек**
  └─ Трек появляется в его списке со статусом "На модерации"
  
✅ **Админ видит трек**
  └─ Трек виден в админ-панели "Модерация треков"
  
✅ **Админ одобряет**
  └─ Статус меняется на "Одобрен"
  └─ Создаётся транзакция (списание коинов)
  
✅ **Артист видит результат**
  └─ Статус обновился до "Одобрен"
  └─ Баланс уменьшился на сумму модерации
  └─ Транзакция отображается в финансах

✅ **Полная синхронизация**
  └─ Данные одинаковы в обоих кабинетах
  └─ Изменения видны мгновенно
  └─ Сохраняются при перезагрузке

---

## 📊 СТАТУС

**Готовность инфраструктуры:** 50%

- ✅ DataContext создан и готов к использованию
- ✅ Интегрирован в RootApp
- ✅ localStorage персистентность работает
- 🚧 Требуется интеграция во все компоненты (50% работы)
- 🚧 Требуется добавить userId логику
- 🚧 Требуется протестировать полные циклы

**Время на завершение:** 30-60 минут активной работы

---

**СЛЕДУЮЩИЙ ШАГ:** Интегрировать useData() во все компоненты артиста и админа

**КРАЙНЕ ВАЖНО:** Без этой интеграции данные НЕ будут синхронизироваться!
