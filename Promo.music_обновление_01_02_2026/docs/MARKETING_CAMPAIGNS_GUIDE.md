# 🎯 Маркетинговые кампании - Руководство

## 🚀 Быстрый старт

Раздел "Маркетинг" полностью работает и готов к использованию! Но для сохранения данных необходимо выполнить SQL миграцию.

---

## ⚠️ Текущий статус

### ✅ Что работает:
- **UI полностью готов** - создание кампаний, выбор слотов, таргетинг, блогеры
- **Backend API готов** - все endpoints настроены и работают
- **Расчёт бюджета** - автоматический подсчёт со скидками
- **Демо-режим** - можно создавать кампании и тестировать интерфейс

### ⚠️ Что нужно:
- **SQL таблица marketing_campaigns** не создана
- **Данные НЕ сохраняются** в базу данных
- После перезагрузки страницы данные теряются

---

## 📋 Установка SQL таблиц (3 минуты)

### Шаг 1: Откройте Supabase Dashboard
```
https://supabase.com/dashboard/project/qzpmiiqfwkcnrhvubdgt
```

### Шаг 2: SQL Editor
В боковом меню выберите **"SQL Editor"** → **"New query"**

### Шаг 3: Выполните SQL
Скопируйте SQL из файла `/supabase/migrations/001_promotion_tables.sql` и выполните.

Или выполните вручную этот SQL:

```sql
-- MARKETING CAMPAIGNS TABLE
CREATE TABLE IF NOT EXISTS marketing_campaigns (
  id TEXT PRIMARY KEY,
  artist_id TEXT NOT NULL,
  campaign_name TEXT NOT NULL,
  campaign_type TEXT NOT NULL
    CHECK (campaign_type IN ('social_ads', 'influencer', 'email', 'content', 'full_package', 'slots_based')),
  target_audience JSONB DEFAULT '{}'::jsonb,
  budget INTEGER NOT NULL,
  duration_days INTEGER DEFAULT 30,
  platforms JSONB DEFAULT '[]'::jsonb,
  goals JSONB DEFAULT '{}'::jsonb,
  status TEXT NOT NULL DEFAULT 'draft'
    CHECK (status IN ('draft', 'pending_approval', 'active', 'paused', 'completed', 'cancelled')),
  metrics JSONB DEFAULT '{}'::jsonb,
  roi DECIMAL(10,2) DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  started_at TIMESTAMPTZ,
  ended_at TIMESTAMPTZ,
  
  -- Новые поля для системы слотов
  description TEXT DEFAULT '',
  content_ids JSONB DEFAULT '[]'::jsonb,
  expected_results JSONB DEFAULT '{}'::jsonb,
  creatives JSONB DEFAULT '[]'::jsonb,
  brandbook_url TEXT DEFAULT '',
  targeting JSONB DEFAULT '{}'::jsonb,
  blogger_slots JSONB DEFAULT '[]'::jsonb,
  selected_slots JSONB DEFAULT '{}'::jsonb
);

CREATE INDEX IF NOT EXISTS idx_marketing_artist ON marketing_campaigns(artist_id);
CREATE INDEX IF NOT EXISTS idx_marketing_status ON marketing_campaigns(status);
CREATE INDEX IF NOT EXISTS idx_marketing_type ON marketing_campaigns(campaign_type);

COMMENT ON TABLE marketing_campaigns IS 'Маркетинговые кампании для продвижения';
```

### Шаг 4: Проверьте
Перейдите в **"Table Editor"** и убедитесь, что таблица `marketing_campaigns` создана.

### Шаг 5: Обновите страницу
Перезагрузите приложение - раздел Маркетинг заработает полностью!

---

## 🎨 Возможности раздела

### 1. **Рекламные слоты** (5 типов)
- 📝 **Пост в ленте** - 5000₽ (классическая публикация)
- 📸 **Stories** - 3000₽ (истории 24 часа)
- 🎬 **Видео** - 15000₽ (полноценное видео)
- ⚡ **Reels/Shorts** - 8000₽ (вертикальные видео)
- 🤝 **Интеграция** - 20000₽ (нативная реклама)

### 2. **Платформы** (5 соцсетей)
- **Instagram** - коэффициент 1.0
- **VK** - коэффициент 0.8 (дешевле)
- **Telegram** - коэффициент 0.7 (самый дешёвый)
- **YouTube** - коэффициент 1.5 (дороже)
- **TikTok** - коэффициент 1.2

### 3. **Работа с блогерами**
- Добавление блогеров с указанием:
  - Платформа (Instagram, VK, Telegram, YouTube, TikTok)
  - Количество подписчиков
  - Категория (Музыка, Хип-хоп, Рок, DJ, Лайфстайл...)
  - Типы контента (пост, stories, видео, reels, интеграция)
  - Стоимость за слот
  - Количество слотов

### 4. **Таргетинг**
- **Возраст** - от 13 до 65 лет
- **Пол** - мужчины, женщины, все
- **География** - 12 городов России (Москва, СПб, Казань...)
- **Интересы** - 16 категорий (Музыка, Хип-хоп, Концерты, DJ...)
- **Lookalike** - поиск похожей аудитории
- **Custom Audiences** - свои списки

### 5. **Загрузка материалов**
- Креативы для рекламы
- Брендбук (ссылка)
- ID контента для продвижения (треки, видео, альбомы)

### 6. **KPI и ожидаемые результаты**
- Охват (reach)
- Вовлечённость (engagement)
- Конверсии (conversions)
- CTR (процент кликов)

### 7. **Скидки по подписке**
```
Basic:  0% (нет скидки)
START:  5% скидка
PRO:    15% скидка
ЭЛИТ:   25% скидка
```

Некоторые слоты входят в подписку бесплатно!

---

## 💰 Примеры расчёта бюджета

### Пример 1: Простая кампания
```
Instagram - Пост в ленте: 5000₽ × 1.0 = 5000₽
Instagram - Stories: 3000₽ × 1.0 = 3000₽
Итого: 8000₽

С подпиской PRO (-15%): 6800₽
```

### Пример 2: Мульти-платформенная кампания
```
Instagram - Reels: 8000₽ × 1.0 = 8000₽
TikTok - Reels: 8000₽ × 1.2 = 9600₽
YouTube - Видео: 15000₽ × 1.5 = 22500₽
+ Блогер @musiclover (100k): 10000₽ × 2 слота = 20000₽
Итого: 60100₽

С подпиской ЭЛИТ (-25%): 45075₽
```

### Пример 3: VK + Telegram (бюджетно)
```
VK - Пост: 5000₽ × 0.8 = 4000₽
VK - Stories: 3000₽ × 0.8 = 2400₽
Telegram - Пост: 5000₽ × 0.7 = 3500₽
Итого: 9900₽

С подпиской START (-5%): 9405₽
```

---

## 🔄 Пошаговый мастер создания

Система использует 5-шаговый wizard:

### Шаг 1: Выбор слотов
- Название кампании
- Описание целевой аудитории
- Длительность (7-90 дней)
- Выбор платформ и типов слотов

### Шаг 2: Контент для продвижения
- ID треков/видео/альбомов
- Тип контента (track, video, album)
- Загрузка креативов
- Ссылка на брендбук

### Шаг 3: Таргетинг
- Возраст (от/до)
- Пол (мужчины/женщины/все)
- География (города)
- Интересы (категории)
- Lookalike и Custom Audiences

### Шаг 4: Блогеры (опционально)
- Добавление блогеров вручную
- Указание стоимости и количества слотов
- Категории и типы контента

### Шаг 5: Обзор и отправка
- Финальная проверка всех параметров
- Автоматический расчёт бюджета
- Применение скидок
- Отправка кампании

---

## 📊 API Endpoints

```typescript
// Получить список кампаний артиста
GET /api/promotion/marketing/:artistId
Response: { success: true, data: MarketingCampaign[], _meta: {...} }

// Создать новую кампанию
POST /api/promotion/marketing/submit
Body: {
  artist_id: string,
  campaign_name: string,
  description: string,
  campaign_type: 'slots_based',
  budget: number,
  duration_days: number,
  platforms: string[],
  content_ids: string[],
  expected_results: {...},
  creatives: string[],
  brandbook_url: string,
  targeting: {...},
  blogger_slots: BloggerSlot[],
  selected_slots: {...}
}
Response: { success: true, data: MarketingCampaign }
```

---

## 🎯 Структура данных

### MarketingCampaign
```typescript
interface MarketingCampaign {
  id: string;
  artist_id: string;
  campaign_name: string;
  campaign_type: string;
  description?: string;
  budget: number;
  duration_days: number;
  platforms: string[];
  content_ids?: string[];
  expected_results?: ExpectedResults;
  creatives?: string[];
  brandbook_url?: string;
  targeting?: TargetingConfig;
  blogger_slots?: BloggerSlot[];
  selected_slots?: Record<string, string[]>;
  status: 'draft' | 'pending_approval' | 'active' | 'paused' | 'completed' | 'cancelled';
  metrics: {
    impressions?: number;
    clicks?: number;
    conversions?: number;
    reach?: number;
    engagement?: number;
  };
  roi: number;
  created_at: string;
  started_at?: string;
  ended_at?: string;
}
```

### BloggerSlot
```typescript
interface BloggerSlot {
  id: string;
  platform: 'instagram' | 'vk' | 'telegram' | 'youtube' | 'tiktok';
  blogger_name: string;
  blogger_link: string;
  subscribers: number;
  category: string;
  content_types: string[];
  price_per_slot: number;
  slots_count: number;
}
```

---

## 🐛 Troubleshooting

### Проблема: "Нет кампаний" при открытии раздела
**Решение:** Это нормально! SQL таблица не создана. Выполните миграцию (см. выше).

### Проблема: Кнопка "Проверить подключение" не находит таблицу
**Решение:** 
1. Проверьте, что SQL выполнен в правильном проекте Supabase
2. Обновите страницу (Ctrl+R)
3. Проверьте в Table Editor, что таблица `marketing_campaigns` существует

### Проблема: Данные не сохраняются
**Решение:** Убедитесь что:
1. SQL таблица создана
2. В консоли браузера нет ошибок
3. Backend логи показывают "✅ Database tables initialized"

### Проблема: Ошибка при создании кампании
**Решение:**
1. Проверьте заполнение обязательных полей (название кампании)
2. Выберите хотя бы один рекламный слот
3. Проверьте консоль браузера на наличие ошибок

---

## 📱 Скриншоты функционала

### Пустое состояние (до SQL миграции)
- ✨ Красивая лендинг-страница с описанием
- 📋 Пошаговые инструкции по настройке
- 💡 Превью цен на рекламные слоты
- 🔗 Прямая ссылка на Supabase Dashboard
- 🎯 Кнопки "Проверить подключение" и "Создать кампанию (демо)"

### После создания таблицы
- 📊 Статистика: всего кампаний, активных, завершено, средний ROI
- 📝 Список всех кампаний с метриками
- ➕ Кнопка создания новой кампании
- 🎨 Детальная информация по каждой кампании

---

## 🚀 Roadmap

### Будущие улучшения:
- [ ] Интеграция с рекламными кабинетами соцсетей
- [ ] Автоматический запуск кампаний через API
- [ ] A/B тестирование креативов
- [ ] Детальная аналитика по дням
- [ ] Рекомендации по оптимизации бюджета
- [ ] Marketplace блогеров с верификацией
- [ ] Шаблоны успешных кампаний
- [ ] Экспорт отчётов в PDF/Excel

---

💡 **Совет:** Начните с небольшой тестовой кампании (1-2 платформы, 7 дней) чтобы понять эффективность каналов для вашей аудитории!

✅ **После выполнения SQL миграции раздел Маркетинг готов к использованию!**
