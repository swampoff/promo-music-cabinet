# ⚡ Быстрый старт promo.music

## 🎯 За 5 минут до запуска

### 1️⃣ Клонирование и установка (1 мин)

```bash
git clone https://github.com/your-username/promo-music.git
cd promo-music
npm install
```

### 2️⃣ Настройка Supabase (2 мин)

1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте новый проект (название: `promo-music`)
3. Скопируйте из **Settings → API**:
   - Project URL
   - anon public key

### 3️⃣ Переменные окружения (1 мин)

Создайте `.env` файл:

```bash
cp .env.example .env
```

Вставьте ваши ключи:

```env
VITE_SUPABASE_URL=https://ваш-проект.supabase.co
VITE_SUPABASE_ANON_KEY=ваш-анон-ключ
```

### 4️⃣ Запуск! (1 мин)

```bash
npm run dev
```

Откройте: **http://localhost:5173** 🎉

---

## 🧪 Тестирование API

### Проверка здоровья сервера

```bash
curl https://ваш-проект.supabase.co/functions/v1/make-server-84730125/health
```

Ответ:
```json
{
  "status": "ok",
  "timestamp": "2024-01-25T12:00:00.000Z"
}
```

### Создание первого трека

Откройте Browser Console (F12) и выполните:

```javascript
// Импортируйте API (уже доступен в приложении)
const { tracksApi } = await import('/src/utils/api.ts');

// Создайте трек
const track = await tracksApi.create({
  title: 'My First Track',
  artist: 'My Name',
  genre: 'Electronic',
  duration: 180,
  coverUrl: 'https://images.unsplash.com/photo-1470225620780-dba8ba36b745?w=400&h=400&fit=crop'
});

console.log('Трек создан:', track);
```

### Получение всех треков

```javascript
const { data } = await tracksApi.getAll();
console.log('Мои треки:', data);
```

---

## 🎨 Кастомизация

### Изменить цветовую схему

Откройте `/src/styles/theme.css` и измените цвета:

```css
:root {
  /* Замените cyan на ваш цвет */
  --color-primary: #FF6B9D;  /* Розовый вместо cyan */
  --color-secondary: #C648C6; /* Фиолетовый вместо blue */
}
```

### Изменить шрифт

В `/src/styles/fonts.css` замените Manrope на другой:

```css
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');

body {
  font-family: 'Inter', sans-serif;
}
```

---

## 📱 Основные страницы

| Страница | Описание | Ключ навигации |
|----------|----------|----------------|
| Главная | Dashboard со статистикой | `'home'` |
| Треки | Список треков | `'tracks'` |
| Альбомы | Список альбомов | `'albums'` |
| Видео | Музыкальные клипы | `'videos'` |
| Концерты | Расписание концертов | `'concerts'` |
| Аналитика | Графики и insights | `'analytics'` |
| Донаты | История пожертвований | `'donations'` |
| Коины | Баланс и транзакции | `'coins'` |
| Новости | Публикации | `'news'` |
| Настройки | Профиль и preferences | `'settings'` |

Навигация в коде:

```typescript
setCurrentPage('tracks'); // Переход на страницу треков
```

---

## 🔧 Полезные команды

```bash
# Dev сервер
npm run dev

# Production сборка
npm run build

# Предпросмотр production
npm run preview

# Проверка типов
npm run type-check
```

---

## 🚀 Деплой на Vercel за 3 минуты

1. **Push в GitHub**

```bash
git add .
git commit -m "Initial commit"
git push origin main
```

2. **Импорт в Vercel**

- Перейдите на [vercel.com](https://vercel.com)
- New Project → Import Git Repository
- Выберите ваш репозиторий

3. **Добавьте переменные**

В Vercel:
- Settings → Environment Variables
- Добавьте `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`

4. **Deploy!**

Нажмите Deploy и дождитесь завершения.

Ваше приложение будет доступно по адресу: `https://ваш-проект.vercel.app`

---

## 📚 Следующие шаги

После запуска изучите:

1. **[API.md](API.md)** - Документация API
2. **[DATABASE.md](DATABASE.md)** - Структура данных
3. **[DEPLOY.md](DEPLOY.md)** - Подробный гид по деплою

---

## ❓ FAQ

### В: Как изменить порт dev сервера?

О: Добавьте в `vite.config.ts`:

```typescript
export default {
  server: {
    port: 3000
  }
}
```

### В: Где хранятся данные?

О: В Supabase KV Store. Все данные изолированы по `userId`.

### В: Как добавить аутентификацию?

О: Используйте Supabase Auth. См. комментарии в коде `/supabase/functions/server/routes.tsx`.

### В: Можно ли использовать без Supabase?

О: Нет, Supabase необходим для backend. Но можно заменить на другой backend (требует переписывания `/src/utils/api.ts`).

### В: Как добавить новую страницу?

О: 
1. Создайте компонент в `/src/app/components/new-page.tsx`
2. Добавьте в `App.tsx` рендеринг
3. Добавьте кнопку в Sidebar

### В: Где добавлять файлы для хранения?

О: Используйте Supabase Storage:

```typescript
const { data } = await supabase.storage
  .from('tracks')
  .upload('file.mp3', file);
```

---

## 🎉 Готово!

Теперь у вас есть полнофункциональный кабинет артиста!

**Что дальше?**
- ✅ Кастомизируйте дизайн под ваш бренд
- ✅ Добавьте реальные треки и данные
- ✅ Подключите платежную систему для донатов
- ✅ Интегрируйте с музыкальными платформами (Spotify API, etc.)
- ✅ Добавьте email уведомления
- ✅ Настройте аналитику (Google Analytics)

**Удачи с проектом! 🚀**
