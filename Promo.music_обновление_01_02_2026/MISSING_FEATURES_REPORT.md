# 📋 ОТЧЁТ: ЧТО НЕ ДОДЕЛАНО В РАЗДЕЛЕ "МОИ КОНЦЕРТЫ"

**Дата анализа:** 26 января 2026  
**Текущая оценка завершенности:** 65% ✅  
**Приоритет доработки:** ВЫСОКИЙ ⚡

---

## 🔍 АНАЛИЗ ТЕКУЩЕГО СОСТОЯНИЯ

### ✅ ЧТО РЕАЛИЗОВАНО (65%):

#### 1. **Базовый CRUD для TourDate** ✅
- [x] Создание концертов
- [x] Редактирование концертов
- [x] Удаление концертов
- [x] Отображение списка предстоящих концертов

#### 2. **Основные поля TourDate** ✅
- [x] `title` - название концерта
- [x] `description` - описание
- [x] `venue_name` - название площадки
- [x] `city` - город
- [x] `date` - дата концерта
- [x] `doors_open` - время открытия дверей
- [x] `show_start` - время начала шоу
- [x] `ticket_url` - ссылка на билеты
- [x] `ticket_price_min` - минимальная цена
- [x] `ticket_price_max` - максимальная цена
- [x] `banner_url` - баннер (в схеме)
- [x] `genre` - жанр (в схеме)

#### 3. **Модерация** ✅
- [x] `moderation_status` (draft, pending, approved, rejected)
- [x] Отправка на модерацию
- [x] Отображение статуса модерации
- [x] `rejection_reason` - причина отклонения

#### 4. **Продвижение** ✅
- [x] `is_promoted` - флаг продвижения
- [x] `promotion_expires_at` - срок действия
- [x] Покупка продвижения за коины

#### 5. **Backend API** ✅
- [x] GET `/tour-dates` - получение списка
- [x] POST `/tour-dates` - создание
- [x] PUT `/tour-dates/:id` - обновление
- [x] DELETE `/tour-dates/:id` - удаление
- [x] POST `/tour-dates/:id/moderate` - модерация
- [x] POST `/tour-dates/:id/promote` - продвижение

---

## ❌ ЧТО НЕ РЕАЛИЗОВАНО (35%):

### 1. **КРИТИЧНО: История выступлений (Performance History)** ❌

**Backend:** ✅ УЖЕ РЕАЛИЗОВАН!
- ✅ GET `/performance-history` - получение истории
- ✅ POST `/performance-history` - добавление
- ✅ PUT `/performance-history/:id` - обновление
- ✅ DELETE `/performance-history/:id` - удаление

**Frontend:** ❌ **НЕТ UI ВООБЩЕ!**

#### Что нужно добавить:

```tsx
// Новая вкладка "История выступлений"
interface PerformanceHistoryItem {
  id: string;
  event_name: string;        // ❌ НЕТ
  venue_name: string;        // ❌ НЕТ
  city: string;              // ❌ НЕТ
  date: string;              // ❌ НЕТ
  audience_size?: number;    // ❌ НЕТ
  type: EventType;           // ❌ НЕТ
  description?: string;      // ❌ НЕТ
  photos?: string[];         // ❌ НЕТ (массив URL фото)
}
```

**Компоненты которых нет:**
- ❌ `<PerformanceHistoryTab />` - вкладка истории
- ❌ `<PerformanceHistoryList />` - список прошлых выступлений
- ❌ `<PerformanceHistoryCard />` - карточка выступления
- ❌ `<PerformanceHistoryModal />` - форма добавления/редактирования
- ❌ `<PerformanceHistoryStats />` - статистика (всего выступлений, городов, аудитория)

**Функции которых нет:**
- ❌ Добавление прошлых концертов
- ❌ Редактирование истории
- ❌ Удаление записей из истории
- ❌ Загрузка фотографий с выступлений
- ❌ Фильтрация по типу события
- ❌ Сортировка по дате
- ❌ Поиск по названию/городу
- ❌ Экспорт истории в PDF

---

### 2. **ВАЖНО: Расширенные поля TourDate** ⚠️

#### Поля которые есть в типах, но НЕ используются:

```typescript
// В database.ts ЕСТЬ, в форме НЕТ:
country: string;                 // ❌ Страна
venue_id?: string;               // ❌ ID площадки
venue_capacity?: number;         // ❌ Вместимость
tickets_sold: number;            // ❌ Продано билетов
event_type: EventType;           // ❌ Тип события
status: TourDateStatus;          // ❌ Статус концерта
photos?: string[];               // ❌ Фотографии
tour_name?: string;              // ❌ Название тура
venue_address?: string;          // ❌ Адрес площадки (есть в схеме, но не в типе)
```

#### Недостающие статусы концерта:

```typescript
// Сейчас используется только moderation_status
// Нужен отдельный status для жизненного цикла концерта:
type TourDateStatus = 
  | 'announced'      // ❌ Анонсировано
  | 'on_sale'        // ❌ В продаже
  | 'sold_out'       // ❌ Распродано
  | 'cancelled'      // ❌ Отменено
  | 'postponed'      // ❌ Перенесено
  | 'completed';     // ❌ Завершено
```

**Что нужно добавить в форму:**
- ❌ Выбор страны (dropdown)
- ❌ Вместимость площадки (number)
- ❌ Тип события (select: Концерт, Фестиваль, Клубное выступление и т.д.)
- ❌ Название тура (если концерт часть тура)
- ❌ Статус концерта (announced, on_sale, sold_out и т.д.)
- ❌ Загрузка фотографий

---

### 3. **ВАЖНО: Интеграция с другими разделами** ⚠️

#### 3.1. Связь с публичным профилем
```typescript
// ArtistProfile.events_calendar - ЕСТЬ в типе, НЕ используется
events_calendar?: any[];  // ❌ НЕТ синхронизации с TourDate
```

**Что должно быть:**
- ❌ Автоматическое добавление одобренных концертов в events_calendar
- ❌ Отображение концертов на публичном профиле
- ❌ Календарь концертов для фанатов
- ❌ iCal экспорт

#### 3.2. Букинг (DjBookingRequest)
```typescript
// Связь с букингом ВООБЩЕ НЕТ
interface DjBookingRequest {
  venue_id: string;
  artist_id: string;
  event_date: string;
  // ...
}
```

**Что должно быть:**
- ❌ Создание TourDate из принятой заявки на букинг
- ❌ Связь TourDate.id <-> DjBookingRequest.id
- ❌ Автоматическое обновление статуса при букинге

#### 3.3. Профиль площадки (VenueProfile)
```typescript
// Связь с площадками НЕТ
interface VenueProfile {
  id: string;
  name: string;
  capacity: number;
  address: string;
  // ...
}
```

**Что должно быть:**
- ❌ venue_id для связи с VenueProfile
- ❌ Автозаполнение данных площадки при выборе
- ❌ Отображение деталей площадки
- ❌ История выступлений на площадке

#### 3.4. Аналитика
```typescript
// Есть поля views/clicks, но нет полной аналитики
interface TourDateAnalytics {
  views: number;              // ✅ ЕСТЬ
  clicks: number;             // ✅ ЕСТЬ
  
  // НЕТ:
  ticket_sales_data?: any;    // ❌ График продаж билетов
  traffic_sources?: any;      // ❌ Источники трафика
  conversion_rate?: number;   // ❌ Конверсия
  revenue?: number;           // ❌ Доход
}
```

**Что должно быть:**
- ❌ Детальная аналитика по каждому концерту
- ❌ График продаж билетов
- ❌ Источники трафика (соц. сети, реклама)
- ❌ Конверсия кликов в продажи
- ❌ Географическое распределение покупателей

---

### 4. **Средний приоритет: Дополнительные функции** 💡

#### 4.1. Управление турами
- ❌ Группировка концертов в туры
- ❌ Создание/редактирование туров
- ❌ Отображение тура как единой сущности
- ❌ Статистика по туру

#### 4.2. Календарь и планирование
- ❌ Календарный вид (не только список)
- ❌ Drag & drop для изменения дат
- ❌ Конфликты расписания (предупреждения)
- ❌ Экспорт в Google Calendar / iCal

#### 4.3. Билетная система
- ❌ Реальный учёт проданных билетов
- ❌ Интеграция с билетными платформами
- ❌ QR-коды билетов
- ❌ Статистика продаж в реальном времени

#### 4.4. Social sharing
- ❌ Шаринг концерта в соц. сети
- ❌ Open Graph meta tags
- ❌ Автопост о новом концерте
- ❌ Уведомления подписчикам

#### 4.5. Фильтрация и поиск
- ❌ Фильтр по статусу концерта (on_sale, sold_out и т.д.)
- ❌ Фильтр по типу события
- ❌ Фильтр по стране/городу
- ❌ Поиск по названию
- ❌ Сортировка (по дате, по продажам и т.д.)

#### 4.6. Bulk операции
- ❌ Массовый выбор концертов
- ❌ Массовое удаление
- ❌ Массовое изменение статуса
- ❌ Массовый экспорт

---

## 📊 СТАТИСТИКА РЕАЛИЗАЦИИ

### Сущности и поля:

```
TourDate поля:
✅ Реализовано:  13/24 (54%)
❌ Не используется: 11/24 (46%)

PerformanceHistory:
✅ Backend:      4/4 (100%)
❌ Frontend:     0/4 (0%)

Интеграции:
✅ Реализовано:  0/4 (0%)
❌ Не реализовано: 4/4 (100%)
```

### Компоненты:

```
✅ Есть:
- ConcertFormModal
- MyConcertsPage
- ConcertCard (в составе страницы)

❌ Нет:
- PerformanceHistoryTab
- PerformanceHistoryModal
- TourManagement
- ConcertCalendarView
- ConcertAnalytics
- VenueSelector
- EventTypeSelector
- CountrySelector
```

---

## 🎯 ПРИОРИТИЗАЦИЯ ЗАДАЧ

### 🔴 КРИТИЧНО (Must Have):

1. **История выступлений (Performance History)** ⚡
   - Срок: 2-3 дня
   - Сложность: Средняя
   - Backend: ✅ Готов
   - Frontend: ❌ Нужно создать полностью

2. **Расширенные поля TourDate** ⚡
   - Срок: 1-2 дня
   - Сложность: Низкая
   - Добавить: country, venue_capacity, event_type, status

3. **Статусы концерта (on_sale, sold_out и т.д.)** ⚡
   - Срок: 1 день
   - Сложность: Низкая
   - Реализовать переключение статусов

### 🟡 ВАЖНО (Should Have):

4. **Связь с публичным профилем**
   - Срок: 2 дня
   - Сложность: Средняя
   - Синхронизация с events_calendar

5. **Базовая аналитика концертов**
   - Срок: 2-3 дня
   - Сложность: Средняя
   - Графики, статистика

6. **Фильтрация и поиск**
   - Срок: 1-2 дня
   - Сложность: Низкая
   - По статусу, типу, городу

### 🟢 ЖЕЛАТЕЛЬНО (Nice to Have):

7. **Интеграция с VenueProfile**
   - Срок: 3-4 дня
   - Сложность: Высокая

8. **Календарный вид**
   - Срок: 2-3 дня
   - Сложность: Средняя

9. **Управление турами**
   - Срок: 3-5 дней
   - Сложность: Высокая

10. **Social sharing**
    - Срок: 1-2 дня
    - Сложность: Низкая

---

## 📦 ПЛАН РЕАЛИЗАЦИИ

### **Фаза 1: Критичный функционал (1 неделя)** 🔴

#### День 1-3: История выступлений
```tsx
// Создать:
- PerformanceHistoryTab.tsx
- PerformanceHistoryModal.tsx
- PerformanceHistoryCard.tsx
- performance-history-adapter.ts

// Функции:
- Добавление прошлых концертов
- Редактирование
- Удаление
- Загрузка фотографий
- Статистика
```

#### День 4-5: Расширенные поля
```tsx
// Добавить в ConcertFormModal:
- CountrySelector (dropdown с флагами)
- VenueCapacity (number input)
- EventTypeSelector (select)
- TourName (text input)
- ConcertStatusSelector (radio/select)

// Обновить:
- concert-schema.ts
- ConcertFormData type
- Backend validation
```

#### День 6-7: Статусы и переходы
```tsx
// Создать:
- ConcertStatusBadge.tsx
- StatusTransitionModal.tsx

// Логика переходов:
announced → on_sale → sold_out
                   → cancelled
                   → postponed → on_sale
                   → completed → performance_history
```

### **Фаза 2: Важные интеграции (1 неделя)** 🟡

#### День 8-10: Публичный профиль
```tsx
// Реализовать:
- Синхронизация TourDate → events_calendar
- Виджет концертов на профиле
- iCal экспорт

// Компоненты:
- PublicConcertCalendar.tsx
- UpcomingShowsWidget.tsx
```

#### День 11-13: Аналитика
```tsx
// Создать:
- ConcertAnalyticsDashboard.tsx
- TicketSalesChart.tsx
- TrafficSourcesPie.tsx
- ConversionMetrics.tsx

// API:
- GET /tour-dates/:id/analytics
```

#### День 14: Фильтры и поиск
```tsx
// Добавить:
- ConcertFilters.tsx
- ConcertSearch.tsx
- Advanced filtering logic
```

### **Фаза 3: Дополнительно (по желанию)** 🟢

```tsx
// Можно добавить позже:
- VenueProfile integration
- Calendar view (react-big-calendar)
- Tour management
- Bulk operations
- Social sharing
```

---

## 💰 ОЦЕНКА ТРУДОЗАТРАТ

```
Фаза 1 (Критично):     40 часов   (1 неделя fulltime)
Фаза 2 (Важно):        40 часов   (1 неделя fulltime)
Фаза 3 (Желательно):   60 часов   (1.5 недели fulltime)

ИТОГО:                 140 часов  (3.5 недели fulltime)
```

---

## 🎯 РЕКОМЕНДАЦИИ

### Начать с:
1. ✅ **История выступлений** - backend готов, нужен только UI
2. ✅ **Расширенные поля** - просто добавить в форму
3. ✅ **Статусы концерта** - важно для жизненного цикла

### Затем:
4. Публичный профиль (для фанатов)
5. Базовая аналитика (для артиста)
6. Фильтрация (для удобства)

### В последнюю очередь:
7. VenueProfile integration
8. Календарный вид
9. Управление турами
10. Social features

---

## 📌 ВЫВОДЫ

**Текущая завершенность раздела: 65%**

**Что работает отлично:**
- ✅ Базовый CRUD
- ✅ Модерация
- ✅ Продвижение
- ✅ Валидация форм
- ✅ UX/UI дизайн

**Что критично добавить:**
- ❌ История выступлений (UI)
- ❌ Расширенные поля TourDate
- ❌ Статусы жизненного цикла концерта
- ❌ Интеграция с профилем

**Минимальный функционал для production:**
- Фаза 1 (1 неделя) - достаточно для базового использования

**Полный функционал:**
- Фаза 1 + 2 + 3 (3.5 недели) - завершённая экосистема

---

**Дата отчёта:** 26 января 2026  
**Следующий шаг:** Реализовать Фазу 1 (История выступлений)
