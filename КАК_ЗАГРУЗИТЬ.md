# 📤 КАК ЗАГРУЗИТЬ ПРОЕКТ В GITHUB

## ⚡ БЫСТРЫЙ СПОСОБ (1 КОМАНДА)

У вас есть 2 автоматических скрипта которые сделают ВСЁ за вас:

### **macOS / Linux:**
```bash
chmod +x deploy.sh
./deploy.sh
```

### **Windows PowerShell:**
```powershell
./deploy.ps1
```

**Скрипт автоматически:**
1. Создаст `.env` файл
2. Инициализирует Git  
3. Добавит все файлы
4. Создаст коммит
5. Запушит в GitHub

---

## 📋 РУЧНОЙ СПОСОБ (если скрипт не работает)

### **Шаг 1: Создайте .env файл**
```bash
cp .env.example .env
```

Откройте `.env` и добавьте ваши Supabase credentials:
```
VITE_SUPABASE_URL=https://qzpmiiqfwkcnrhvubdgt.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGc...ваш-ключ
```

### **Шаг 2: Git init**
```bash
git init
git remote add origin https://github.com/swampoff/promofm.git
```

### **Шаг 3: Commit**
```bash
git add .
git commit -m "🎵 Initial commit: Promo.Music"
```

### **Шаг 4: Push**
```bash
git branch -M main
git push -u origin main --force
```

---

## 🔑 ГДЕ ВЗЯТЬ SUPABASE CREDENTIALS:

1. **Откройте:** https://supabase.com/dashboard
2. **Выберите проект** (ваш проект уже создан: `qzpmiiqfwkcnrhvubdgt`)
3. **Settings** → **API**
4. **Скопируйте:**
   - Project URL: `https://qzpmiiqfwkcnrhvubdgt.supabase.co`
   - anon public key: `eyJhbGc...` (длинная строка)

---

## ✅ ЧТО БУДЕТ ЗАГРУЖЕНО:

Все 80+ файлов проекта:
- ✅ `/src/` - весь исходный код (React компоненты, hooks, utils)
- ✅ `/supabase/` - Edge Functions (backend)
- ✅ `/public/` - статические файлы
- ✅ Конфигурация (package.json, vite.config.ts, vercel.json, tsconfig.json)
- ✅ .gitignore, .env.example, LICENSE
- ✅ README.md и документация

---

## 🚨 ВОЗМОЖНЫЕ ОШИБКИ:

### ❌ "Permission denied"
**Решение:**
```bash
# Настройте Git credentials
git config --global user.name "ваше-имя"
git config --global user.email "ваш-email"

# Или используйте Personal Access Token
git remote set-url origin https://YOUR_TOKEN@github.com/swampoff/promofm.git
```

### ❌ "Repository not found"
**Решение:** Убедитесь что репозиторий существует на github.com/swampoff/promofm

### ❌ "deploy.sh: permission denied"
**Решение:**
```bash
chmod +x deploy.sh
```

---

## 📞 СЛЕДУЮЩИЕ ШАГИ:

После загрузки в GitHub:

1. **Откройте Vercel:** https://vercel.com/new
2. **Import:** `swampoff/promofm`
3. **Add Env Variables:**
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
4. **Deploy!**

**Подробная инструкция:** [БЫСТРЫЙ_СТАРТ.md](БЫСТРЫЙ_СТАРТ.md)

---

**Made with ❤️ by AI Assistant** 🚀
