# 🔄 РЕАЛИЗАЦИЯ СИСТЕМЫ ОБРАТНОЙ СВЯЗИ ДЛЯ ПИТЧИНГА

> **Дата:** 2026-02-01  
> **Версия:** 2.0  
> **Статус:** В разработке

---

## 📋 ОБЗОР

Реализуем полноценную систему обратной связи с двумя каналами получения ответов от получателей рассылок (радиостанции, блогеры, заведения, лейблы).

---

## 🎯 ЦЕЛИ

### Канал №1: Автоматизированный (Мини-портал)
- ✅ Уникальная ссылка в каждом письме
- ✅ Брендированная страница с информацией о треке
- ✅ Плеер для прослушивания
- ✅ Форма обратной связи с быстрыми кнопками
- ✅ Автоматическое сохранение в базу данных

### Канал №2: Ручной ввод администратором
- ✅ Поиск рассылки и получателя в админке
- ✅ Ручное обновление статуса
- ✅ Добавление комментария

### Отображение для администратора:
- ✅ Лента отзывов в реальном времени
- ✅ Детализированная таблица получателей
- ✅ Сводная статистика по артисту
- ✅ График лучших каналов
- ✅ История всех отзывов

### Отображение для артиста:
- ✅ Дашборд с ключевыми достижениями
- ✅ Воронка конверсии (Отправлено → Прослушано → В ротации)
- ✅ География прослушиваний (карта)
- ✅ Анонимная статистика
- ✅ Лучшие отзывы (без контактов базы)

---

## 🏗️ АРХИТЕКТУРА

### Обновленные типы данных:

```typescript
// Статусы получателя
export type RecipientStatus = 
  | 'sent'                    // Отправлено
  | 'opened'                  // Открыто письмо
  | 'downloaded'              // Скачан файл
  | 'responded_positive'      // Взяли в ротацию
  | 'responded_neutral'       // Рассмотрим позже
  | 'responded_negative';     // Не подходит

// Тип ответа
export type FeedbackType = 'positive' | 'neutral' | 'negative';

// Получатель
export interface Recipient {
  id: string;
  name: string;                    // Радиостанция/блог/заведение
  contactPerson?: string;
  email: string;
  phone?: string;
  city?: string;
  category: string;                // федеральное/региональное и т.д.
  status: RecipientStatus;
  feedbackDate?: string;
  feedback?: FeedbackResponse;
}

// Обратная связь
export interface FeedbackResponse {
  id: string;
  recipientId: string;
  distributionId: string;
  itemId: number;
  type: FeedbackType;
  message?: string;
  respondedAt: string;
  respondedVia: 'portal' | 'manual';
  addedBy?: string;                // userId админа (для ручного ввода)
  
  // Дополнительная информация
  willRotate?: boolean;            // Возьмут в ротацию?
  rotationStartDate?: string;
  playlistName?: string;
  estimatedReach?: number;
}

// Обновленный PitchingDistribution
export interface PitchingDistribution {
  id: string;
  direction: PitchingDirection;
  baseId: string;
  baseName: string;
  filesCount: number;
  sentDate: string;
  comment?: string;
  
  // НОВОЕ: Массив получателей с их статусами
  recipients: Recipient[];
  
  // Метрики (рассчитываются автоматически)
  recipientsCount: number;
  openRate?: number;               // % открытых
  downloadRate?: number;           // % скачавших
  responseRate?: number;           // % ответивших
  positiveRate?: number;           // % положительных ответов
}
```

---

## 📁 СТРУКТУРА КОМПОНЕНТОВ

### 1. Мини-портал обратной связи (Публичный)

**Файл:** `/src/feedback/FeedbackPortal.tsx`

**URL:** `/feedback/:distributionId/:recipientId`

**Функциональность:**
- Отображение информации о треке (обложка, артист, название)
- Плеер для прослушивания (если есть аудио)
- Кнопки скачивания файлов
- Форма обратной связи:
  - Быстрые кнопки: [👍 Взяли в ротацию] [🤔 Рассмотрим] [👎 Не подходит]
  - Текстовое поле для комментария
  - Дополнительные поля (зависят от типа получателя):
    - Для радио: дата начала ротации, название программы
    - Для заведений: название плейлиста
    - Для блогов: ссылка на публикацию
- Отправка данных в localStorage/backend
- Сообщение об успешной отправке

**UI/UX:**
- Брендированный дизайн в стиле promo.music
- Glassmorphism эффекты
- Адаптивный (320px → desktop)
- Анимации при отправке

---

### 2. Админ-панель: Детальный просмотр рассылки

**Файл:** `/src/admin/pages/DistributionDetail.tsx`

**Функциональность:**
- Хедер с информацией о рассылке:
  - Артист, трек, дата отправки
  - Направление (радио/заведения/СМИ/лейблы)
  - База рассылки
- 5 карточек статистики:
  - Всего отправлено
  - Открыто (%)
  - Скачано (%)
  - Ответили (%)
  - Положительных ответов (%)
- **Таблица получателей:**
  - Колонки: Название | Город | Статус | Дата ответа | Комментарий | Действия
  - Фильтр по статусу
  - Поиск по названию
  - Сортировка
  - Действия на строку:
    - Просмотр детальной информации
    - Ручной ввод ответа
    - Отправить повторно (если не ответили)
- **Лента отзывов в реальном времени:**
  - "14:30, Radio FM: 👍 Взяли в ротацию"
  - "15:15, Music Blog: 🤔 Рассмотрим позже. Комментарий: '...'"
  - Последние 10 ответов
  - Сортировка по дате (новые сверху)

**Модалы:**
1. **Просмотр детальной информации получателя**
   - Полная информация о получателе
   - История всех взаимодействий
   - Статистика откликов (сколько раз брали в ротацию)

2. **Ручной ввод ответа**
   - Выбор типа ответа (positive/neutral/negative)
   - Текстовое поле для комментария
   - Дополнительные поля (willRotate, rotationStartDate, и т.д.)
   - Кнопка "Сохранить"

---

### 3. Админ-панель: Статистика по артисту

**Файл:** `/src/admin/pages/ArtistPitchingStats.tsx`

**Функциональность:**
- Выбор артиста (dropdown или поиск)
- **Сводная статистика:**
  - Всего питчей: 12
  - Всего рассылок: 48
  - Всего отправлено: 1,250 получателям
  - Всего ответов: 387 (31%)
  - Положительных: 124 (32% от ответивших)
  - Нейтральных: 198 (51%)
  - Отрицательных: 65 (17%)
- **График "Лучшие каналы":**
  - Гистограмма по направлениям (радио, заведения, СМИ, лейблы)
  - Показывает % положительных откликов для каждого направления
  - Интерактивный (hover показывает детали)
- **График "Динамика откликов":**
  - Линейный график по времени
  - Показывает рост положительных откликов
- **Таблица "История отзывов":**
  - Все отзывы по всем материалам артиста
  - Фильтр по типу (positive/neutral/negative)
  - Фильтр по направлению
  - Фильтр по дате

---

### 4. Кабинет артиста: Дашборд питчинга

**Файл:** `/src/app/pages/PitchingDashboard.tsx`

**Функциональность:**
- **Главный дашборд:**
  - Ключевые достижения (карточки):
    - "🎉 Ваш трек 'Я делаю шаг' в ротации на Radio FM!"
    - "🎶 Трек добавлен в плейлист сети кофеен 'City Coffee'"
    - "📰 Ваш релиз упомянут в 5 музыкальных блогах"
  - Воронка конверсии:
    - Отправлено: 250 контактам
    - Прослушано: 150 (60%)
    - Попало в плейлисты/ротацию: 8 (5.3% от прослушанных)
- **География прослушиваний:**
  - Карта России/мира
  - Точки в городах, где трек был прослушан
  - Hover показывает количество прослушиваний в городе
- **Анонимная статистика:**
  - "Ваш трек был прослушан 50 музыкальными редакторами"
  - "30 кураторами плейлистов"
  - "Средняя оценка: 7.8/10" (если есть оценки)
- **Лучшие отзывы:**
  - Администратор может "закрепить" позитивные комментарии
  - Отображаются БЕЗ указания источника (конфиденциальность)
  - Макс 5-7 лучших отзывов

**Ограничения:**
- ❌ НЕТ доступа к контактам базы
- ❌ НЕТ имен получателей
- ❌ НЕТ email/телефонов
- ✅ Только агрегированная статистика
- ✅ Только анонимные отзывы

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Генерация уникальной ссылки:

```typescript
// В момент создания рассылки генерируем токен для каждого получателя
const generateFeedbackToken = (distributionId: string, recipientId: string) => {
  return btoa(`${distributionId}:${recipientId}:${Date.now()}`);
};

// URL в письме:
const feedbackUrl = `https://promo.music/feedback/${token}`;
```

### Сохранение ответа (автоматический канал):

```typescript
// В FeedbackPortal.tsx
const handleSubmitFeedback = async (type: FeedbackType, message?: string) => {
  const feedback: FeedbackResponse = {
    id: `feedback_${Date.now()}`,
    recipientId,
    distributionId,
    itemId,
    type,
    message,
    respondedAt: new Date().toISOString(),
    respondedVia: 'portal',
    // Дополнительные поля из формы...
  };

  // Сохранить в localStorage (или отправить на backend)
  saveFeedback(feedback);
  
  // Обновить статус получателя
  updateRecipientStatus(recipientId, `responded_${type}`);
  
  // Показать сообщение об успехе
  showSuccessMessage();
};
```

### Ручной ввод (админ):

```typescript
// В DistributionDetail.tsx
const handleManualFeedback = (recipientId: string, feedbackData: any) => {
  const feedback: FeedbackResponse = {
    id: `feedback_${Date.now()}`,
    recipientId,
    distributionId,
    itemId,
    type: feedbackData.type,
    message: feedbackData.message,
    respondedAt: new Date().toISOString(),
    respondedVia: 'manual',
    addedBy: currentAdminUserId, // ID текущего админа
    ...feedbackData.additional,
  };

  // Сохранить отзыв
  addFeedbackToRecipient(recipientId, feedback);
  
  // Обновить статус
  updateRecipientStatus(recipientId, `responded_${feedbackData.type}`);
};
```

### Расчет метрик:

```typescript
// Автоматический расчет при обновлении PitchingDistribution
const calculateMetrics = (recipients: Recipient[]) => {
  const total = recipients.length;
  const opened = recipients.filter(r => 
    ['opened', 'downloaded', 'responded_positive', 'responded_neutral', 'responded_negative']
    .includes(r.status)
  ).length;
  const downloaded = recipients.filter(r => 
    ['downloaded', 'responded_positive', 'responded_neutral', 'responded_negative']
    .includes(r.status)
  ).length;
  const responded = recipients.filter(r => r.feedback).length;
  const positive = recipients.filter(r => 
    r.feedback?.type === 'positive'
  ).length;

  return {
    recipientsCount: total,
    openRate: (opened / total) * 100,
    downloadRate: (downloaded / total) * 100,
    responseRate: (responded / total) * 100,
    positiveRate: responded > 0 ? (positive / responded) * 100 : 0,
  };
};
```

---

## 📊 МОКОВЫЕ ДАННЫЕ

Создам файл `/src/data/mockPitchingFeedback.ts` с:
- 50+ моковых получателей для разных рассылок
- 30+ моковых отзывов (positive/neutral/negative)
- Реалистичные названия радиостанций, блогов, заведений
- Города по всей России
- Разнообразные комментарии

---

## 🎨 UI КОМПОНЕНТЫ

### Компоненты для переиспользования:

1. **StatusBadge** - бейджи статусов получателей
2. **FeedbackCard** - карточка отзыва
3. **RecipientRow** - строка таблицы получателя
4. **StatCard** - карточка статистики
5. **ConversionFunnel** - воронка конверсии
6. **GeoMap** - карта с точками (можно использовать react-simple-maps)
7. **FeedbackTimeline** - лента отзывов в реальном времени
8. **ChannelChart** - график лучших каналов (recharts)

---

## 📅 ПЛАН РЕАЛИЗАЦИИ

### Фаза 1: Типы и моковые данные ✅
- [x] Обновить типы в DataContext
- [x] Создать интерфейсы Recipient и FeedbackResponse
- [ ] Создать моковые данные

### Фаза 2: Мини-портал обратной связи
- [ ] Создать страницу FeedbackPortal
- [ ] Добавить форму обратной связи
- [ ] Реализовать сохранение ответов
- [ ] Добавить роутинг `/feedback/:token`

### Фаза 3: Админ - Детальный просмотр рассылки
- [ ] Создать страницу DistributionDetail
- [ ] Добавить таблицу получателей
- [ ] Реализовать ленту отзывов
- [ ] Добавить модалы (просмотр, ручной ввод)

### Фаза 4: Админ - Статистика по артисту
- [ ] Создать страницу ArtistPitchingStats
- [ ] Добавить графики (recharts)
- [ ] Реализовать фильтрацию и поиск

### Фаза 5: Кабинет артиста - Дашборд
- [ ] Создать страницу PitchingDashboard
- [ ] Добавить воронку конверсии
- [ ] Реализовать карту с географией
- [ ] Добавить раздел "Лучшие отзывы"

### Фаза 6: Интеграция и тестирование
- [ ] Связать все компоненты с DataContext
- [ ] Добавить real-time обновления (если нужно)
- [ ] Протестировать все сценарии
- [ ] Создать документацию

---

## 🔐 БЕЗОПАСНОСТЬ И ПРИВАТНОСТЬ

### Для артистов:
- ❌ Скрыть все контакты получателей
- ❌ Скрыть email/телефоны
- ❌ Скрыть имена радиостанций/блогов (только агрегированная статистика)
- ✅ Показывать только анонимные отзывы (закрепленные админом)
- ✅ Показывать только города (без конкретных адресов)

### Для админов:
- ✅ Полный доступ ко всем данным
- ✅ Возможность закреплять отзывы для артистов
- ✅ Логирование всех ручных изменений (addedBy поле)

---

## 📈 МЕТРИКИ УСПЕХА

### KPI системы:
- Отклик (Response Rate): цель >20%
- Положительные отзывы: цель >30% от откликнувшихся
- Использование портала vs ручной ввод: цель 70/30

### Аналитика:
- Какие направления дают лучший отклик?
- Какие артисты получают больше положительных откликов?
- В какое время суток приходит больше ответов?

---

**Дата:** 2026-02-01  
**Статус:** 🟡 В разработке  
**Прогресс:** 10% (типы данных обновлены)

---

**Made with ❤️ for PROMO.MUSIC**
