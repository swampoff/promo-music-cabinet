# 🎯 Все команды для деплоя PROMO.MUSIC

## ⚡ One-Click Setup (САМОЕ ПРОСТОЕ!)

```bash
chmod +x setup.sh
./setup.sh
```

**Что произойдёт:**
- ✅ Настроит Git
- ✅ Настроит GitHub Secrets
- ✅ Подготовит всё для автодеплоя
- ✅ Проверит что всё готово

**Время:** 5 минут

---

## 🔧 Пошаговая настройка

### 1. Git и первый push

```bash
chmod +x scripts/init-git.sh
./scripts/init-git.sh
```

### 2. GitHub Secrets

```bash
chmod +x scripts/setup-secrets.sh
./scripts/setup-secrets.sh
```

### 3. Полный деплой (вручную)

```bash
# Установите токены
export VERCEL_TOKEN=your-token
export SUPABASE_ACCESS_TOKEN=your-token

# Запустите
chmod +x scripts/deploy-all.sh
./scripts/deploy-all.sh --production
```

---

## 🚀 Быстрые команды

### Деплой всего (Frontend + Backend):

```bash
export VERCEL_TOKEN=xxx
export SUPABASE_ACCESS_TOKEN=xxx

./scripts/deploy-all.sh --prod
```

### Только Frontend:

```bash
export VERCEL_TOKEN=xxx

./scripts/deploy-frontend.sh --prod
```

### Только Backend:

```bash
export SUPABASE_ACCESS_TOKEN=xxx

./scripts/deploy-backend.sh
```

---

## 🔍 Проверка

### Проверка Frontend:

```bash
curl https://your-project.vercel.app
```

### Проверка Backend:

```bash
curl https://qzpmiiqfwkcnrhvubdgt.supabase.co/functions/v1/make-server-84730125/health
```

### Проверка GitHub Actions:

```bash
# Откройте в браузере
open https://github.com/YOUR-USERNAME/promo-music/actions
```

---

## 📦 Получение токенов

### Vercel Token:

```bash
# 1. Откройте
open https://vercel.com/account/tokens

# 2. Create Token
# 3. Скопируйте токен

export VERCEL_TOKEN=your-token-here
```

### Vercel Project IDs:

```bash
# 1. Откройте проект на Vercel
# 2. Settings → General
# 3. Скопируйте Organization ID и Project ID

export VERCEL_ORG_ID=team_xxx
export VERCEL_PROJECT_ID=prj_xxx
```

### Supabase Token:

```bash
# 1. Откройте
open https://supabase.com/dashboard/account/tokens

# 2. Generate new token
# 3. Скопируйте токен

export SUPABASE_ACCESS_TOKEN=sbp_xxx
```

---

## 🔄 Автоматический деплой

После настройки, просто:

```bash
# Внесите изменения в код
# ...

# Закоммитьте
git add .
git commit -m "Добавил новую фичу"

# Push
git push

# GitHub Actions автоматически задеплоит!
# Проверьте статус:
# https://github.com/YOUR-USERNAME/promo-music/actions
```

---

## 🛠️ Дополнительные команды

### Проверка локально перед деплоем:

```bash
# Установите зависимости
npm install

# Запустите dev сервер
npm run dev

# Откройте http://localhost:5173

# Build для проверки
npm run build

# Preview build
npm run preview
```

### Откат деплоя (Vercel):

```bash
# Список деплоев
vercel list

# Откат к предыдущему
vercel rollback
```

### Логи Edge Functions (Supabase):

```bash
# Откройте в браузере
open https://supabase.com/dashboard/project/qzpmiiqfwkcnrhvubdgt/functions
```

---

## 📊 Мониторинг

### Vercel Analytics:

```bash
# Откройте проект → Analytics
open https://vercel.com/[your-username]/promo-music/analytics
```

### GitHub Actions History:

```bash
# Откройте Actions tab
open https://github.com/YOUR-USERNAME/promo-music/actions
```

### Supabase Logs:

```bash
# Edge Functions logs
open https://supabase.com/dashboard/project/qzpmiiqfwkcnrhvubdgt/logs/edge-functions
```

---

## 🐛 Troubleshooting

### Сделать скрипты исполняемыми:

```bash
chmod +x setup.sh
chmod +x scripts/*.sh
```

### Проверка environment variables:

```bash
echo $VERCEL_TOKEN
echo $SUPABASE_ACCESS_TOKEN
```

### Переустановка зависимостей:

```bash
rm -rf node_modules
npm install
```

### Очистка кэша:

```bash
npm cache clean --force
rm -rf node_modules dist .vercel
npm install
```

---

## 📝 Шпаргалка

```bash
# ONE-CLICK SETUP
./setup.sh

# MANUAL DEPLOY (всё сразу)
export VERCEL_TOKEN=xxx
export SUPABASE_ACCESS_TOKEN=xxx
./scripts/deploy-all.sh --prod

# AUTO-DEPLOY (после настройки)
git push  # Всё остальное автоматически!

# ПРОВЕРКА
curl https://qzpmiiqfwkcnrhvubdgt.supabase.co/functions/v1/make-server-84730125/health
```

---

## 🎯 Быстрый старт (Copy-Paste)

```bash
# 1. Настройка за 5 минут
chmod +x setup.sh && ./setup.sh

# 2. Получите токены:
# Vercel: https://vercel.com/account/tokens
# Supabase: https://supabase.com/dashboard/account/tokens

# 3. Настройте GitHub Secrets (если не сделали в setup.sh)
./scripts/setup-secrets.sh

# 4. Подключите Vercel к GitHub
# https://vercel.com/new

# 5. Готово! Теперь при каждом push:
git push  # Автодеплой!
```

---

## 📚 Документация

- **Полная инструкция:** `./АВТОДЕПЛОЙ.md`
- **Быстрый деплой:** `./БЫСТРЫЙ_ДЕПЛОЙ.md`
- **Деплой на домен:** `./ДЕПЛОЙ_НА_ДОМЕН.md`

---

**Успешного деплоя! 🚀**
