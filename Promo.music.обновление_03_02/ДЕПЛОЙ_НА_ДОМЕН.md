# 🚀 Деплой PROMO.MUSIC на свой домен

## 📋 Что нужно перед началом

- ✅ Ваш домен (например: `promo-music.ru` или `music.yourdomain.com`)
- ✅ Доступ к настройкам DNS домена
- ✅ Аккаунт на платформе деплоя (Vercel/Netlify) - **бесплатно!**
- ✅ Проект PROMO.MUSIC (у вас уже есть)

---

## 🎯 РЕКОМЕНДУЕМЫЙ СПОСОБ: Vercel (Самый простой!)

**Почему Vercel:**
- ✅ Бесплатный план (достаточно для старта)
- ✅ Автоматический SSL сертификат
- ✅ CDN по всему миру
- ✅ Автоматические деплои из Git
- ✅ Custom domain за 2 минуты
- ✅ Поддержка Vite из коробки

---

## 📦 ЭТАП 1: Подготовка проекта

### Шаг 1.1: Создайте GitHub репозиторий

**В GitHub:**

1. Перейдите на https://github.com/new
2. Название: `promo-music` (или любое)
3. Описание: `Кабинет артиста с аналитикой и донатами`
4. Visibility: **Private** (рекомендуется)
5. Нажмите **"Create repository"**

### Шаг 1.2: Загрузите код в GitHub

**В терминале вашего проекта:**

```bash
# Инициализируйте git (если ещё не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: PROMO.MUSIC готов к деплою"

# Свяжите с GitHub (замените на ваш URL)
git remote add origin https://github.com/ваш-username/promo-music.git

# Отправьте код
git branch -M main
git push -u origin main
```

**✅ Готово!** Код теперь на GitHub.

---

## 🌐 ЭТАП 2: Деплой на Vercel

### Шаг 2.1: Создайте аккаунт Vercel

1. Перейдите на https://vercel.com/signup
2. Нажмите **"Continue with GitHub"**
3. Разрешите доступ Vercel к вашим репозиториям

### Шаг 2.2: Импортируйте проект

1. На dashboard Vercel нажмите **"Add New..." → "Project"**
2. Найдите репозиторий `promo-music`
3. Нажмите **"Import"**

### Шаг 2.3: Настройте проект

**На странице настройки:**

#### Framework Preset:
- Выберите: **Vite**

#### Build Settings:
```
Build Command:      npm run build
Output Directory:   dist
Install Command:    npm install
```

#### Environment Variables:
Нажмите **"Add Environment Variable"** и добавьте:

```
VITE_SUPABASE_URL=https://qzpmiiqfwkcnrhvubdgt.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InF6cG1paXFmd2tjbnJodnViZGd0Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3Mzc3MTkwOTMsImV4cCI6MjA1MzI5NTA5M30.9wCGmE8k7oIcqDDLUiOZfVV-rT4S2B2KX7qC1KFG_YE
```

### Шаг 2.4: Деплой!

Нажмите **"Deploy"**

**Процесс (2-3 минуты):**
```
1. Installing dependencies... ⏳
2. Building...                ⏳
3. Deploying...               ⏳
4. ✅ Success!
```

### Шаг 2.5: Проверьте результат

После деплоя вы получите URL:
```
https://promo-music-xxxxxxx.vercel.app
```

**Откройте его!** Вы должны увидеть ваше приложение! 🎉

---

## 🌍 ЭТАП 3: Подключение вашего домена

### Шаг 3.1: Добавьте домен в Vercel

**В Vercel Dashboard:**

1. Откройте ваш проект `promo-music`
2. Перейдите на вкладку **"Settings"**
3. В левом меню выберите **"Domains"**
4. Нажмите **"Add Domain"**
5. Введите ваш домен: `promo-music.ru` (или ваш)
6. Нажмите **"Add"**

**Vercel покажет DNS записи, которые нужно добавить.**

---

### Шаг 3.2: Настройте DNS

**У вашего регистратора домена (REG.RU, Timeweb, Cloudflare и т.д.):**

#### Вариант A: CNAME запись (рекомендуется для поддомена)

Если ваш домен: `music.yourdomain.com`

```
Type:  CNAME
Name:  music
Value: cname.vercel-dns.com
TTL:   3600
```

#### Вариант B: A запись (для основного домена)

Если ваш домен: `promo-music.ru`

```
Type:  A
Name:  @
Value: 76.76.21.21
TTL:   3600
```

**И также добавьте:**
```
Type:  CNAME
Name:  www
Value: cname.vercel-dns.com
TTL:   3600
```

### Шаг 3.3: Дождитесь распространения DNS

**Обычно занимает:**
- 5-10 минут (быстрые DNS провайдеры)
- До 24 часов (медленные)

**Проверить можно так:**
```bash
# В терминале
nslookup promo-music.ru

# Или онлайн
https://www.whatsmydns.net/
```

### Шаг 3.4: SSL сертификат

**Vercel автоматически:**
- ✅ Выпустит бесплатный SSL сертификат (Let's Encrypt)
- ✅ Настроит HTTPS
- ✅ Настроит редирект с HTTP на HTTPS

**Ничего делать не нужно!** Просто подождите 5-10 минут.

---

## ✅ ЭТАП 4: Проверка работы

### Откройте ваш домен:

```
https://promo-music.ru
```

**Вы должны увидеть:**
- ✅ Тёмный фон с glassmorphism
- ✅ Боковое меню работает
- ✅ Графики загружаются
- ✅ Зелёный замочек в адресной строке (SSL)
- ✅ Быстрая загрузка (CDN)

### Проверьте функционал:

1. **Навигация** → переключение страниц работает
2. **Аналитика** → графики отображаются
3. **Система коинов** → модалка открывается
4. **API** → откройте F12 → Network → должны быть запросы к Supabase

---

## 🔧 НАСТРОЙКА SUPABASE (ВАЖНО!)

### Разрешите ваш домен в Supabase

**Это важно для CORS!**

1. Перейдите на https://supabase.com/dashboard/project/qzpmiiqfwkcnrhvubdgt
2. **Settings** → **API**
3. Прокрутите до **Site URL**
4. Добавьте ваш домен: `https://promo-music.ru`
5. В **Additional Redirect URLs** добавьте:
   ```
   https://promo-music.ru/*
   https://www.promo-music.ru/*
   ```
6. Нажмите **"Save"**

### Обновите CORS в Edge Functions

Файл `/supabase/functions/server/index.tsx` уже настроен:

```typescript
app.use('*', cors({
  origin: '*', // Разрешить все домены для демо
  credentials: true,
}));
```

**Для production** рекомендуется изменить на:

```typescript
app.use('*', cors({
  origin: [
    'https://promo-music.ru',
    'https://www.promo-music.ru',
    'http://localhost:5173', // Для разработки
  ],
  credentials: true,
}));
```

**Передеплойте Edge Functions:**
```bash
supabase functions deploy make-server-84730125
```

---

## 🔄 АВТОМАТИЧЕСКИЕ ОБНОВЛЕНИЯ

### Настройте CI/CD (автоматический деплой при изменениях)

**Vercel уже настроил это автоматически!**

**Теперь при каждом git push:**

```bash
# Внесите изменения в код
# ...

# Закоммитьте
git add .
git commit -m "Обновил дизайн главной страницы"

# Отправьте в GitHub
git push origin main
```

**Vercel автоматически:**
1. ✅ Обнаружит изменения в GitHub
2. ✅ Запустит build
3. ✅ Задеплоит новую версию
4. ✅ Обновит ваш домен

**Время: 2-3 минуты от push до обновления сайта!**

---

## 📊 МОНИТОРИНГ И АНАЛИТИКА

### Vercel Analytics (бесплатно)

**В Vercel Dashboard:**

1. Откройте проект `promo-music`
2. Вкладка **"Analytics"**
3. Нажмите **"Enable Analytics"**

**Вы увидите:**
- 📈 Количество посетителей
- ⚡ Скорость загрузки страниц
- 🌍 География пользователей
- 📱 Устройства (desktop/mobile)

### Добавьте Google Analytics (опционально)

**Создайте файл `/src/analytics.ts`:**

```typescript
// Google Analytics 4
export const initGA = () => {
  if (typeof window !== 'undefined') {
    // Вставьте ваш GA4 код
    const script = document.createElement('script');
    script.src = 'https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX';
    script.async = true;
    document.head.appendChild(script);

    (window as any).dataLayer = (window as any).dataLayer || [];
    function gtag(...args: any[]) {
      (window as any).dataLayer.push(arguments);
    }
    gtag('js', new Date());
    gtag('config', 'G-XXXXXXXXXX');
  }
};
```

**В `/src/main.tsx` добавьте:**

```typescript
import { initGA } from './analytics';

// После рендера
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// Инициализируйте аналитику
initGA();
```

---

## 🚀 АЛЬТЕРНАТИВНЫЕ ПЛАТФОРМЫ

### Вариант 2: Netlify

**Похож на Vercel, тоже бесплатно:**

1. https://app.netlify.com/signup
2. **"Add new site"** → **"Import an existing project"**
3. Выберите GitHub репозиторий
4. Build settings:
   ```
   Build command:   npm run build
   Publish folder:  dist
   ```
5. Environment variables: те же что для Vercel
6. **"Deploy site"**
7. **Settings → Domain management** → Add custom domain

**DNS настройки:**
```
Type:  CNAME
Name:  www
Value: [ваш-сайт].netlify.app

Type:  A
Name:  @
Value: 75.2.60.5
```

---

### Вариант 3: Свой сервер (VPS)

**Для опытных пользователей!**

#### Требования:
- Ubuntu 22.04 / Debian 11+
- Nginx
- Node.js 20+
- SSL сертификат (Let's Encrypt)

#### Краткие шаги:

```bash
# 1. Соберите проект
npm run build

# 2. Скопируйте dist/ на сервер
scp -r dist/* user@your-server:/var/www/promo-music/

# 3. Настройте Nginx
# /etc/nginx/sites-available/promo-music

server {
    listen 80;
    server_name promo-music.ru www.promo-music.ru;
    
    root /var/www/promo-music;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Gzip
    gzip on;
    gzip_types text/css application/javascript application/json;
}

# 4. Установите SSL
sudo certbot --nginx -d promo-music.ru -d www.promo-music.ru

# 5. Перезапустите Nginx
sudo systemctl reload nginx
```

**Подробная инструкция:** `/ДЕПЛОЙ_VPS.md` (создам если нужно)

---

## 🔒 БЕЗОПАСНОСТЬ

### Обязательные настройки:

#### 1. Environment Variables (секреты)

**НИКОГДА не коммитьте в Git:**
- ❌ API ключи
- ❌ Пароли от БД
- ❌ Секретные токены

**Используйте:**
- ✅ `.env.local` (добавлен в `.gitignore`)
- ✅ Vercel Environment Variables
- ✅ Supabase Service Role Key только на сервере

#### 2. Настройте `.gitignore`

Проверьте что есть:
```
node_modules/
dist/
.env
.env.local
.env.production
```

#### 3. CORS настройки

В production измените на конкретные домены (см. выше)

#### 4. Rate Limiting

**В Supabase Edge Functions добавьте:**

```typescript
import { RateLimiter } from 'hono/rate-limiter';

app.use(
  '/api/*',
  RateLimiter({
    windowMs: 15 * 60 * 1000, // 15 минут
    max: 100, // Макс 100 запросов
  })
);
```

---

## 📈 ОПТИМИЗАЦИЯ ПРОИЗВОДИТЕЛЬНОСТИ

### 1. Включите кэширование

**В `vite.config.ts` уже настроено:**
```typescript
build: {
  rollupOptions: {
    output: {
      manualChunks: {
        vendor: ['react', 'react-dom'],
        ui: ['@radix-ui/react-dialog', /* ... */],
      }
    }
  }
}
```

### 2. Оптимизируйте изображения

**Используйте WebP формат:**
```bash
# Конвертируйте PNG/JPG в WebP
npm install -g sharp-cli
sharp input.png -o output.webp
```

### 3. Lazy Loading

**Для больших компонентов:**
```typescript
import { lazy, Suspense } from 'react';

const AnalyticsPage = lazy(() => import('@/app/components/analytics-page'));

// В App.tsx
<Suspense fallback={<div>Загрузка...</div>}>
  <AnalyticsPage />
</Suspense>
```

### 4. Vercel Speed Insights

**Бесплатно!**

```bash
npm install @vercel/speed-insights
```

```typescript
// В App.tsx
import { SpeedInsights } from '@vercel/speed-insights/react';

export default function App() {
  return (
    <>
      <YourApp />
      <SpeedInsights />
    </>
  );
}
```

---

## 🐛 TROUBLESHOOTING

### Проблема: 404 на всех страницах кроме главной

**Причина:** SPA routing не настроен

**Решение для Vercel:**

Создайте `vercel.json` (уже есть в проекте):
```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

**Для Netlify:**

Создайте `public/_redirects`:
```
/*    /index.html   200
```

---

### Проблема: CORS ошибка при запросах к Supabase

**Решение:**

1. Проверьте что домен добавлен в Supabase (см. выше)
2. Обновите CORS в Edge Functions
3. Передеплойте functions:
   ```bash
   supabase functions deploy make-server-84730125
   ```

---

### Проблема: Environment variables не работают

**Vercel:**
1. Settings → Environment Variables
2. Добавьте переменные
3. **Важно:** Redeploy проект после добавления!

**В коде используйте:**
```typescript
const url = import.meta.env.VITE_SUPABASE_URL;
```

---

### Проблема: Медленная загрузка

**Проверьте:**

1. **Lighthouse (Chrome DevTools):**
   - F12 → Lighthouse → Generate report
   - Должен быть Score > 90

2. **Vercel Analytics:**
   - Смотрите на TTFB (должно быть < 200ms)

3. **Оптимизируйте:**
   - Lazy loading компонентов
   - Сжатие изображений
   - Code splitting

---

## 📋 ЧЕКЛИСТ ДЕПЛОЯ

### Перед деплоем:
- [ ] Код протестирован локально
- [ ] `npm run build` работает без ошибок
- [ ] Environment variables подготовлены
- [ ] `.gitignore` настроен
- [ ] Supabase Edge Functions задеплоены

### Деплой:
- [ ] Код загружен на GitHub
- [ ] Проект импортирован в Vercel
- [ ] Environment variables добавлены
- [ ] Build успешный
- [ ] Vercel URL работает

### Custom Domain:
- [ ] Домен добавлен в Vercel
- [ ] DNS записи настроены
- [ ] SSL сертификат выпущен
- [ ] Домен работает с HTTPS
- [ ] Домен добавлен в Supabase

### Проверка:
- [ ] Все страницы загружаются
- [ ] Навигация работает
- [ ] API запросы успешны
- [ ] Нет ошибок в консоли
- [ ] Мобильная версия работает

---

## 🎉 ГОТОВО!

**Ваш сайт теперь доступен по адресу:**
```
https://promo-music.ru
```

**Что работает:**
- ✅ Быстрая загрузка (CDN)
- ✅ SSL сертификат (зелёный замочек)
- ✅ Автоматические обновления при git push
- ✅ Аналитика посетителей
- ✅ Доступен из любой точки мира

---

## 📞 ПОДДЕРЖКА

### Документация:
- Vercel: https://vercel.com/docs
- Supabase: https://supabase.com/docs
- Vite: https://vitejs.dev/guide/

### Полезные ссылки:
- Vercel Dashboard: https://vercel.com/dashboard
- Supabase Dashboard: https://supabase.com/dashboard
- GitHub Repo: https://github.com/ваш-username/promo-music

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

1. **Настройте мониторинг** (Vercel Analytics)
2. **Добавьте Google Analytics**
3. **Настройте email уведомления** (Supabase Auth)
4. **Создайте staging окружение** (отдельный branch)
5. **Настройте backup БД** (Supabase auto-backup)

---

**Поздравляю с успешным деплоем!** 🎊🎵✨

**Ваш кабинет артиста теперь онлайн и доступен для всех клиентов и посетителей!**
