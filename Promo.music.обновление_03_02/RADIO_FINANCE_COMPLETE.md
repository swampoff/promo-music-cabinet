# 📻 RADIO STATION: FINANCE SYSTEM - COMPLETE! ✅

**Дата:** 2 февраля 2026  
**Статус:** Максимально доработан — Enterprise финансовая система  

---

## ✨ ЧТО СОЗДАНО

### **1. SQL СТРУКТУРА** ✅
- **Файл:** `/database/15_radio_financial_system.sql`
- **Таблиц:** 3 основных + расширение users
- **Функций:** 5 (начисление, вывод, обработка, завершение, статистика)
- **Представлений:** 1 (radio_transaction_details)
- **Триггеров:** 1 (финансовые снимки)

### **2. КОМПОНЕНТЫ** ✅
- `/src/radio/components/finance-section.tsx` - Основной компонент
- `/src/radio/components/finance-section-parts.tsx` - Дополнительные компоненты (табы, модальные окна)

### **3. ИНТЕГРАЦИЯ** ✅
- Обновлен `/src/radio/RadioApp.tsx`
- Импорт компонента финансов
- Роутинг к разделу "Финансы"

---

## 🗄️ SQL СТРУКТУРА

### **Таблицы:**

#### **1. users.balance** (Расширение)
```sql
ALTER TABLE users ADD COLUMN balance DECIMAL(12, 2) DEFAULT 0 NOT NULL;
```
- Текущий баланс пользователя (радиостанции)
- Обновляется при завершении транзакций

#### **2. balance_transactions** (История движения средств)
```sql
- id, user_id, user_email
- transaction_type (donation, royalty, withdrawal, fee, refund, bonus, adjustment)
- amount (+ для пополнения, - для вывода)
- description
- related_entity_type, related_entity_id (ссылки на заказы/выводы)
- status (pending, completed, failed, cancelled)
- balance_before, balance_after
- metadata (JSONB)
- created_at, completed_at
```

#### **3. withdrawal_requests** (Заявки на вывод)
```sql
- id, user_id, user_email, user_name
- amount (минимум 500 ₽)
- payment_method (bank_transfer, yoomoney, card, qiwi, webmoney)
- payment_details (JSONB - реквизиты)
- status (pending, approved, processing, completed, rejected, cancelled)
- admin_notes, rejection_reason
- processed_by, processed_date, completed_date
- transaction_id (ссылка на BalanceTransaction)
- payment_confirmation_id, payment_receipt_url
```

#### **4. radio_financial_snapshots** (Ежедневные снимки)
```sql
- id, radio_id, snapshot_date
- daily_revenue, daily_net_revenue, daily_commission, daily_orders
- total_revenue, total_net_revenue, total_commission, total_orders
- ending_balance
- daily_withdrawals, total_withdrawals
```

---

## 💰 ФИНАНСОВАЯ ЛОГИКА

### **Поток средств:**

#### **1. Начисление средств (Royalty)**
```sql
-- Сценарий A: Рекламный заказ
AdvertisementOrder.status = 'fulfilled' →
  credit_radio_from_order(user_id, 'advertisement', order_id, net_amount) →
    User.balance += net_amount_to_radio (85%)
    BalanceTransaction создается (type: 'royalty', amount: +net_amount)

-- Сценарий B: Заказ ротации
RotationOrder.status = 'completed' →
  credit_radio_from_order(user_id, 'rotation', order_id, net_amount) →
    User.balance += net_amount_to_radio (85%)
    BalanceTransaction создается (type: 'royalty', amount: +net_amount)
```

**Формула:**
```javascript
payment_amount = 50000 ₽ (от покупателя)
commission_amount = 50000 × 0.15 = 7,500 ₽ (платформе)
net_amount_to_radio = 50000 × 0.85 = 42,500 ₽ (радио)
```

#### **2. Вывод средств**
```sql
-- Шаг 1: Создание заявки
create_withdrawal_request(user_id, amount, payment_method, payment_details) →
  Проверки:
    - amount >= 500 ₽
    - amount <= available_balance
  Создает WithdrawalRequest (status: 'pending')
  Создает BalanceTransaction (type: 'withdrawal', amount: -amount, status: 'pending')

-- Шаг 2: Обработка админом
process_withdrawal_request(withdrawal_id, admin_id, 'approve'|'reject', notes) →
  Если approve: status = 'approved'
  Если reject: status = 'rejected', BalanceTransaction отменяется

-- Шаг 3: Завершение выплаты
complete_withdrawal_request(withdrawal_id, payment_confirmation_id) →
  WithdrawalRequest.status = 'completed'
  BalanceTransaction.status = 'completed'
  User.balance -= amount (списание происходит ЗДЕСЬ)
```

**Правила:**
- Минимум: 500 ₽
- Комиссия платформы за вывод: 0 ₽ (не берется)
- Доступный баланс = `balance - pending_withdrawals`
- Обработка: 1-3 рабочих дня

---

## 📊 ФУНКЦИИ SQL

### **1. credit_radio_from_order()**
Начисление средств из заказа:
- Блокирует строку user для предотвращения race condition
- Создает BalanceTransaction (type: 'royalty')
- Обновляет User.balance
- Возвращает transaction_id

### **2. create_withdrawal_request()**
Создание заявки на вывод:
- Проверяет доступный баланс
- Проверяет минимальную сумму (500 ₽)
- Создает WithdrawalRequest и BalanceTransaction (pending)
- Возвращает withdrawal_id

### **3. process_withdrawal_request()**
Обработка админом:
- Одобрение: status → 'approved'
- Отклонение: status → 'rejected', транзакция отменяется
- Сохраняет admin_notes, rejection_reason

### **4. complete_withdrawal_request()**
Завершение выплаты:
- Обновляет status → 'completed'
- Списывает деньги с баланса
- Сохраняет payment_confirmation_id

### **5. get_radio_financial_stats()**
Получение статистики за период:
- Валовой/чистый доход, комиссия
- Количество заказов, средний чек
- Текущий/доступный баланс
- Выводы (в обработке, завершено)
- Рост в процентах (сравнение с предыдущим периодом)

---

## 🎨 КОМПОНЕНТЫ UI

### **4 Раздела:**

#### **1. Обзор (Overview)**
- Большая карточка баланса с кнопкой показать/скрыть
- Рост за период (процент с индикатором)
- Доступно к выводу / В обработке / Выведено всего
- График динамики дохода (последние 7 дней)
- Структура дохода (валовой - комиссия = чистый)
- Ключевые метрики

#### **2. Транзакции (Transactions)**
- Фильтрация по типу (все, доход, вывод, бонус, возврат)
- Поиск по описанию
- Список транзакций с карточками:
  - Иконка по типу (цветная)
  - Описание, дата/время, статус
  - Сумма (зеленая/оранжевая для +/-)
  - Баланс после транзакции
- Клик → модальное окно с деталями

#### **3. Выводы (Withdrawals)**
- Фильтрация по статусу
- Карточки заявок:
  - Сумма, метод вывода
  - Статус (цветной бейдж)
  - Даты создания/завершения
  - Реквизиты (скрываемые)
  - ID платежа (копирование)
  - Админские заметки/причина отклонения
- Действия: Отменить (для pending), Скачать чек

#### **4. Аналитика (Analytics)**
Расширенная аналитика (в разработке)

---

## 📈 СТАТИСТИКА

### **7 Индикаторов:**
1. **Валовой доход** - до вычета комиссии (зеленый)
2. **Чистый доход** - после комиссии 15% (синий)
3. **Комиссия платформы** - 15% от всех заказов (оранжевый)
4. **Средний чек** - totalNetRevenue / totalOrders (фиолетовый)

### **3 Балансовых показателя:**
1. **Доступно к выводу** - balance - pending_withdrawals (зеленый)
2. **В обработке** - pending + approved withdrawals (желтый)
3. **Выведено всего** - completed withdrawals (серый)

### **Рост:**
- Сравнение с предыдущим периодом
- Процент изменения
- Зеленая стрелка вверх / красная вниз

---

## 🔄 WORKFLOW

### **Поступление средств:**
```
1. Покупатель оплачивает рекламный заказ → PaymentTransaction
2. AdvertisementOrder.status = 'paid'
3. Радио выполняет заказ
4. Радио отмечает status = 'fulfilled'
5. Автоматически вызывается credit_radio_from_order():
   - User.balance += net_amount_to_radio (85%)
   - BalanceTransaction (type: 'royalty', amount: +42500)
6. Радио видит обновленный баланс в разделе "Финансы"
```

### **Вывод средств:**
```
1. Радио нажимает "Вывести средства"
2. Выбирает сумму (≥500₽), метод вывода, заполняет реквизиты
3. Создается WithdrawalRequest (status: 'pending')
4. BalanceTransaction (type: 'withdrawal', status: 'pending')
5. Баланс НЕ списывается сразу (доступный баланс уменьшается на pending)
6. Админ рассматривает заявку → Одобряет/Отклоняет
7. Если одобрена:
   - Выплата через платежную систему
   - complete_withdrawal_request() → status: 'completed'
   - User.balance -= amount (списание ЗДЕСЬ)
8. Если отклонена:
   - WithdrawalRequest.status = 'rejected'
   - BalanceTransaction отменяется
   - Баланс не меняется
```

---

## 🎯 API ENDPOINTS (TODO)

```typescript
// Финансы
GET    /api/radio/financial-stats?period=30
GET    /api/radio/balance
GET    /api/radio/transactions
GET    /api/radio/transactions/:id

// Выводы
POST   /api/radio/withdrawal-requests
GET    /api/radio/withdrawal-requests
GET    /api/radio/withdrawal-requests/:id
PUT    /api/radio/withdrawal-requests/:id/cancel

// Админ (обработка выводов)
GET    /api/admin/withdrawal-requests
PUT    /api/admin/withdrawal-requests/:id/process
PUT    /api/admin/withdrawal-requests/:id/complete
```

---

## 🔐 БЕЗОПАСНОСТЬ

### **Race Condition Protection:**
```sql
SELECT balance FROM users WHERE id = ? FOR UPDATE;
-- Блокирует строку до завершения транзакции
```

### **Валидация:**
- Минимальная сумма вывода: 500 ₽
- Проверка доступного баланса перед выводом
- Невозможность двойного списания (pending транзакции)
- CHECK constraints на уровне БД

### **Аудит:**
- Все транзакции логируются в balance_transactions
- balance_before и balance_after для отслеживания
- metadata (JSONB) для дополнительной информации
- Связь с источником (related_entity_type/id)

---

## 📊 ОТЧЕТНОСТЬ

### **SQL Запросы:**

**1. Доход за период:**
```sql
SELECT 
  SUM(net_amount_to_radio) as total_net_revenue,
  COUNT(*) as completed_orders
FROM advertisement_orders
WHERE seller_radio_id = ? AND status IN ('paid', 'fulfilled')
  AND created_at >= NOW() - INTERVAL '30 days';
```

**2. Топ-5 пакетов по доходу:**
```sql
SELECT 
  p.title, COUNT(ao.id) as orders, SUM(ao.net_amount_to_radio) as revenue
FROM advertisement_orders ao
JOIN radio_advertisement_packages p ON ao.package_id = p.id
WHERE ao.seller_radio_id = ?
GROUP BY p.id ORDER BY revenue DESC LIMIT 5;
```

**3. Динамика по дням:**
```sql
SELECT 
  DATE(created_at) as date,
  SUM(net_amount_to_radio) as daily_revenue,
  COUNT(*) as daily_orders
FROM advertisement_orders
WHERE seller_radio_id = ? AND status IN ('paid', 'fulfilled')
GROUP BY DATE(created_at) ORDER BY date;
```

---

## 🎨 ДИЗАЙН

### **Цветовая схема:**
- **Положительные операции:** Зеленый (доход, бонусы)
- **Отрицательные операции:** Оранжевый (вывод, комиссии)
- **Статусы выводов:**
  - Pending: Желтый
  - Approved: Синий
  - Processing: Индиго
  - Completed: Зеленый
  - Rejected: Красный
  - Cancelled: Серый

### **Анимации:**
- Motion/React transitions между табами
- Fade-in для модальных окон
- Прогресс-бары с анимацией заполнения
- Пульсация для кнопок действий

### **Адаптивность:**
- **Mobile:** < 640px (вертикальный стек, упрощенные карточки)
- **Tablet:** 640px - 1024px (2 колонки)
- **Desktop:** > 1024px (полная раскладка, 4 колонки статистики)

---

## ✅ РЕЗУЛЬТАТ

**Создана максимально функциональная enterprise-система финансов для радиостанций!**

### **Что включает:**

✅ **SQL:** 3 таблицы + 5 функций + 1 представление + 1 триггер  
✅ **UI:** 4 раздела (Обзор, Транзакции, Выводы, Аналитика)  
✅ **Финансовая логика:** Автоматическое начисление (85%) + вывод средств  
✅ **Workflow:** 6 этапов от заказа до вывода  
✅ **Безопасность:** Race condition protection, валидация, аудит  
✅ **Отчетность:** SQL запросы для аналитики  
✅ **Адаптивность:** Mobile-first дизайн  

### **Монетизация:**
- Автоматическое начисление 85% от каждого заказа
- Платформа берет 15% комиссии
- Минимальный вывод: 500 ₽
- Комиссия за вывод: 0 ₽
- Обработка: 1-3 рабочих дня

---

**Радиостанции теперь могут профессионально управлять финансами и выводить заработанные средства!** 📻💰✨
