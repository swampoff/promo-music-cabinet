# 🎸 ЛОГИКА РАЗДЕЛА "МОИ КОНЦЕРТЫ"

## 📋 ОБЗОР

Раздел "Мои концерты" - это центральный хаб для управления живыми выступлениями артиста. Функционал включает создание, редактирование, модерацию, продвижение концертов и ведение истории выступлений.

---

## 🎯 ОСНОВНЫЕ СУЩНОСТИ

### 1. TourDate (Концерт/Выступление)

**Назначение:** Хранит информацию о предстоящем или прошедшем концерте

**Ключевые поля:**
```typescript
{
  // Идентификация
  id: UUID,
  artist_id: UUID,  // Связь с артистом
  
  // Основная информация
  title: "Summer Fest 2026",
  description: "Грандиозный летний фестиваль",
  tour_name: "World Tour 2026",  // Опционально
  event_type: "Фестиваль",
  
  // Место
  venue_name: "Олимпийский",
  venue_id: UUID,  // Связь с VenueProfile (будущее)
  city: "Москва",
  country: "Россия",
  
  // Время
  date: "2026-06-15",
  doors_open: "18:00",  // Опционально
  show_start: "19:00",
  
  // Билеты
  ticket_url: "https://...",
  ticket_price_min: 2000,
  ticket_price_max: 8000,
  venue_capacity: 30000,
  tickets_sold: 15400,
  
  // Статусы
  status: "on_sale",  // draft, announced, on_sale, sold_out, cancelled, postponed, completed
  moderation_status: "approved",  // draft, pending, approved, rejected
  rejection_reason: "...",  // Если rejected
  
  // Медиа
  banner_url: "https://...",
  photos: ["url1", "url2"],
  
  // Аналитика
  views: 15400,
  clicks: 850,
  
  // Продвижение
  is_promoted: true,
  promotion_expires_at: "2026-06-30",
  
  // Метаданные
  created_at: "...",
  updated_at: "..."
}
```

### 2. ArtistProfile (Профиль Артиста)

**Назначение:** Хранит общую информацию об артисте и его историю

**Ключевые поля для концертов:**
```typescript
{
  // Performance History - массив прошлых выступлений
  performance_history: [
    {
      id: "perf-1",
      event_name: "Rock Fest 2025",
      venue_name: "Arena",
      city: "Москва",
      date: "2025-08-15",
      audience_size: 10000,
      type: "Фестиваль",
      description: "Отличное выступление",
      photos: ["url1", "url2"],
      created_at: "..."
    }
  ],
  
  // Events Calendar - календарь событий
  events_calendar: [...],
  
  // EPK & Riders
  epk_url: "https://...",
  tech_rider_url: "https://...",
  hospitality_rider_url: "https://..."
}
```

---

## 🔄 ЖИЗНЕННЫЙ ЦИКЛ КОНЦЕРТА

### 1. Создание (Draft)

```typescript
// Артист создает концерт
POST /api/concerts/tour-dates
{
  title: "My Concert",
  venue_name: "Venue",
  city: "Moscow",
  date: "2026-06-15",
  show_start: "19:00",
  event_type: "Концерт"
}

// Статус: draft
// moderation_status: draft
```

**UI:**
- Форма с полями
- Загрузка баннера
- Валидация данных
- Кнопка "Сохранить как черновик"

### 2. Отправка на модерацию

```typescript
// Артист отправляет на проверку
POST /api/concerts/tour-dates/:id/submit

// moderation_status: pending
```

**Логика модерации:**
- Модератор проверяет контент
- Проверка баннера (размер, качество)
- Проверка описания (no spam)
- Проверка даты (не в прошлом)

**Результаты:**
- ✅ **Approved** - концерт опубликован
- ❌ **Rejected** - с причиной отклонения

### 3. Публикация (Approved)

```typescript
// После одобрения
// moderation_status: approved
// status: announced -> on_sale

// Концерт виден публике
// Доступен для продвижения
```

**UI:**
- Бейдж "Одобрено"
- Кнопка "Продвинуть"
- Публичная страница доступна

### 4. Продвижение

```typescript
// Артист платит коины за продвижение
POST /api/concerts/tour-dates/:id/promote
{
  days: 7  // Количество дней
}

// Стоимость: 100 коинов за 7 дней
// is_promoted: true
// promotion_expires_at: +7 days
```

**Что дает продвижение:**
- Показ в топе списка концертов
- Специальный бейдж "Продвигается"
- Приоритет в поиске
- Показ на главной странице

### 5. Завершение

```typescript
// После концерта артист добавляет в историю
POST /api/concerts/performance-history
{
  event_name: "Summer Fest 2026",
  venue_name: "Олимпийский",
  city: "Москва",
  date: "2026-06-15",
  audience_size: 28000,
  type: "Фестиваль",
  description: "Отличный концерт!",
  photos: [...]
}

// tour_dates.status: completed
// Добавлено в artist_profiles.performance_history
```

---

## 📊 СТАТУСЫ И ПЕРЕХОДЫ

### Moderation Status Flow

```
draft → pending → approved ✅
                → rejected ❌
                
Переходы:
1. draft → pending:   submit()
2. pending → approved: модератор одобряет
3. pending → rejected: модератор отклоняет
4. rejected → pending: артист исправляет и отправляет снова
5. draft → draft:      сохранение изменений
```

### Event Status Flow

```
draft → announced → on_sale → sold_out → completed
          ↓           ↓           ↓
      cancelled  cancelled  cancelled
          ↓           ↓
      postponed  postponed

Переходы:
1. draft → announced:  публикация после одобрения
2. announced → on_sale: билеты в продаже
3. on_sale → sold_out:  все билеты проданы
4. sold_out → completed: концерт прошел
5. * → cancelled:        отмена концерта
6. * → postponed:        перенос концерта
```

---

## 🔒 БЕЗОПАСНОСТЬ

### Row Level Security (RLS)

**Правила доступа:**

```sql
-- Артист видит только свои концерты
CREATE POLICY "Artists can view own concerts"
  ON tour_dates FOR SELECT
  USING (artist_id = auth.uid());

-- Публика видит только одобренные концерты
CREATE POLICY "Public can view approved concerts"
  ON tour_dates FOR SELECT
  USING (
    moderation_status = 'approved' 
    AND status NOT IN ('draft', 'cancelled')
  );

-- Только владелец может изменять
CREATE POLICY "Artists can update own concerts"
  ON tour_dates FOR UPDATE
  USING (artist_id = auth.uid());
```

### Authentication Flow

```typescript
// 1. Получить access token
const { data: { session } } = await supabase.auth.getSession();
const accessToken = session?.access_token;

// 2. Отправить запрос с токеном
fetch('/api/concerts/tour-dates', {
  headers: {
    'Authorization': `Bearer ${accessToken}`
  }
});

// 3. Backend проверяет токен
const { data: { user } } = await supabase.auth.getUser(accessToken);
if (!user) return 401;

// 4. Выполняется запрос от имени user.id
```

---

## 📈 АНАЛИТИКА

### Tracked Metrics

```typescript
{
  views: 15400,    // Просмотры страницы концерта
  clicks: 850,     // Клики по кнопке "Купить билет"
  tickets_sold: 28000,  // Проданных билетов
  
  // Рассчитываемые метрики:
  conversion_rate: clicks / views * 100,  // % конверсии
  sales_rate: tickets_sold / venue_capacity * 100  // % заполненности
}
```

### События для аналитики

```typescript
// При просмотре концерта
analytics.track('concert_viewed', {
  concert_id: 'uuid',
  artist_id: 'uuid',
  source: 'search' | 'promoted' | 'direct'
});

// При клике на билеты
analytics.track('ticket_click', {
  concert_id: 'uuid',
  price_range: [2000, 8000]
});

// При покупке билета (если интегрирована платежка)
analytics.track('ticket_purchased', {
  concert_id: 'uuid',
  quantity: 2,
  total_price: 4000
});
```

---

## 🎨 UI КОМПОНЕНТЫ

### 1. MyConcertsPage

**Главная страница раздела**

```typescript
<MyConcertsPage 
  userCoins={1250}
  onCoinsUpdate={(coins) => setCoins(coins)}
/>
```

**Функционал:**
- Список всех концертов артиста
- Фильтры по статусу
- Поиск
- Сортировка
- CRUD операции
- Статистика

### 2. ConcertCard

**Карточка концерта**

```typescript
<ConcertCard
  concert={concert}
  onEdit={(id) => ...}
  onDelete={(id) => ...}
  onSubmit={(id) => ...}
  onPromote={(id) => ...}
/>
```

**Отображает:**
- Баннер
- Название, дату, место
- Статус модерации
- Статус события
- Статистику (views, clicks, tickets)
- Действия (edit, delete, submit, promote)

### 3. CreateConcertModal (TODO)

**Форма создания концерта**

```typescript
<CreateConcertModal
  isOpen={showModal}
  onClose={() => setShowModal(false)}
  onSubmit={async (data) => {
    await concertsApiAdapter.create(data);
  }}
/>
```

**Поля:**
- Название *
- Описание
- Дата *
- Время начала *
- Площадка *
- Город *
- Тип события *
- Цена билетов
- Ссылка на билеты
- Баннер (загрузка)

---

## 🔗 ИНТЕГРАЦИИ

### Будущие интеграции

1. **VenueProfile**
   - Связь concert.venue_id → venue_profiles.id
   - Автозаполнение данных площадки
   - Карта и маршруты

2. **BookingSystem**
   - Заявки от промоутеров
   - Автоматическое создание концертов из букингов
   - Контракты и райдеры

3. **SocialMedia**
   - Автопубликация анонсов
   - Интеграция с Facebook Events
   - Instagram Stories

4. **TicketingServices**
   - Реальная продажа билетов
   - Синхронизация tickets_sold
   - Автоматическое обновление статуса

5. **Calendar Apps**
   - Экспорт в Google Calendar
   - Экспорт в Apple Calendar
   - iCal формат

6. **EPK Generator**
   - Автоматическое добавление концертов в EPK
   - Генерация tour dates списка
   - PDF экспорт

---

## 🎯 USER STORIES

### US-1: Создание концерта
```
Как артист
Я хочу добавить информацию о своем концерте
Чтобы фанаты знали где и когда я выступаю

Acceptance Criteria:
✅ Могу ввести название, дату, место
✅ Могу загрузить баннер
✅ Могу сохранить как черновик
✅ Могу отправить на модерацию
```

### US-2: Продвижение концерта
```
Как артист
Я хочу продвинуть свой концерт за коины
Чтобы больше людей его увидели

Acceptance Criteria:
✅ Вижу стоимость продвижения
✅ Могу выбрать срок (7/14/30 дней)
✅ Коины списываются с баланса
✅ Концерт получает приоритет в списке
```

### US-3: История выступлений
```
Как артист
Я хочу вести историю своих выступлений
Чтобы показать свой опыт

Acceptance Criteria:
✅ Могу добавить прошедший концерт
✅ Указываю размер аудитории
✅ Добавляю фото с выступления
✅ История отображается в профиле
```

---

## 📊 МЕТРИКИ УСПЕХА

### KPI

1. **Активность артистов**
   - Количество созданных концертов/месяц
   - % одобренных концертов
   - Среднее время от создания до публикации

2. **Вовлеченность аудитории**
   - Просмотры концертов
   - CTR на кнопку билетов
   - Конверсия в покупку (если есть интеграция)

3. **Монетизация**
   - Продвижений концертов/месяц
   - Средний чек (коины за продвижение)
   - Retention артистов

4. **Качество**
   - % отклоненных концертов
   - Среднее время модерации
   - Количество жалоб

---

## 🔮 ROADMAP

### Phase 1: MVP ✅ (Текущий)
- [x] CRUD концертов
- [x] Модерация
- [x] Продвижение
- [x] Базовая аналитика
- [x] История выступлений

### Phase 2: Enhancement (Месяц 1-2)
- [ ] Форма создания/редактирования
- [ ] Календарь событий
- [ ] Фильтры и поиск
- [ ] Публичная страница концерта
- [ ] Уведомления

### Phase 3: Integrations (Месяц 3-4)
- [ ] VenueProfile integration
- [ ] Social media автопубликация
- [ ] Calendar export (iCal, Google, Apple)
- [ ] Ticketing services API
- [ ] EPK generator

### Phase 4: Advanced (Месяц 5-6)
- [ ] Букинг система
- [ ] Контракты и райдеры
- [ ] Финансовая отчетность
- [ ] Аналитика продаж
- [ ] Machine Learning рекомендации

---

## 📝 ПРИМЕЧАНИЯ

### Технические детали

- **Database:** PostgreSQL (через Supabase)
- **Backend:** Deno Edge Functions (Hono)
- **Frontend:** React + TypeScript
- **Styling:** Tailwind CSS v4
- **Animations:** Framer Motion
- **State:** React Hooks (локальный state)

### Performance

- **Lazy Loading:** Концерты загружаются постранично
- **Optimistic UI:** Действия показываются мгновенно
- **Error Recovery:** Автоматический retry при сбоях
- **Offline Support:** (TODO) Service Worker кеширование

### Accessibility

- **Keyboard Navigation:** Все кнопки доступны с клавиатуры
- **Screen Reader:** ARIA метки на всех элементах
- **Color Contrast:** WCAG AA compliance
- **Focus Management:** Правильный focus flow

---

**Дата:** 26 января 2026  
**Версия:** 1.0  
**Статус:** Documented & Ready ✅
